# 세션 로그 — 2026-04-09

## 작업 요약

OSS 모델 자체 호스팅을 위한 전체 파이프라인 — 현황 분석 → 모델 후보 선정 → SDD 작성 → 3+1 합의 → SDD v2 개정 → 단계 0~1 코드 구현 + 인프라 구축 + sanity check 통과.

## 주요 의사결정

### 1. OSS 모델 자체 호스팅 방향 결정

사용자가 Claude API를 OSS 모델로 대체(문제 출제/관리 한정)하겠다고 결정.
- **실행 방식**: 자체 호스팅 (패턴 X — Ollama 컨테이너 분리, ADR-005 미발동)
- **추론 엔진**: Ollama
- **Claude**: 개발 도구(Claude Code)로 계속 유지

### 2. 서버 환경 발견 (결정적 분기점)

```
GPU: NVIDIA GB10 (Grace Blackwell, aarch64)
메모리: 119 GiB (CPU/GPU 통합 LPDDR5X)
디스크: 1.9 TB NVMe (1.7 TB 가용)
OS: Ubuntu 24.04.4 LTS
CUDA: 13.0, driver 580.126.09
```

→ 일반 24/48GB GPU 시나리오가 아닌 200B급 모델까지 가능한 환경. 후보 선정 기준이 완전히 바뀜.

### 3. 후보 모델 선정 (5개)

1차 후보 6개 → 라이센스/Ollama 검증 → 2개 제외 + 1개 추가 = **5개 확정**

| # | 모델 | 역할 | 제외/변경 |
|---|---|---|---|
| M1 | EXAONE 4.0 32B | 한국어 SOTA | GGUF 수동 import (Ollama 정식 미지원) |
| M2 | EXAONE 3.5 32B | 한국어 베이스라인 + M1 폴백 | Ollama 정식 ✅ |
| M3 | Qwen3-Coder-Next (80B MoE) | 코딩 SOTA | Ollama ≥ 0.15.5 |
| M4 | Qwen2.5-Coder 32B | 코드 베이스라인 | Ollama 정식 ✅ |
| M5 | Llama 3.3 70B | 음의 베이스라인 | Ollama 정식 ✅ |

**제외**:
- Solar Pro 2: 가중치 비공개 (proprietary, API 전용)
- HyperCLOVA X SEED: Naver가 GGUF/Ollama 지원 계획 없다고 명시

### 4. 사용자 결정 (Q1~Q4)

| 결정 | 선택 |
|---|---|
| Q1 평가 하네스 | **promptfoo** (코드 ~70% 절감) |
| Q2 Gold Set B 크기 | **30문제** |
| Q3 EXAONE 라이센스 | 평가 진행 + 운영 시점에만 LG 문의 |
| Q4 Langfuse | **self-host** (데이터 주권) |

### 5. Langfuse v3 풀 인프라 결정

단계 1 sanity check 시 Langfuse v3가 `CLICKHOUSE_URL is not configured`으로 restart loop 발견. v3는 5개 컨테이너 풀 인프라(clickhouse + redis + minio + worker + web) 필수. 사용자가 옵션 B(v3 풀 인프라) 채택.

## 산출 문서

| 문서 | 경로 |
|---|---|
| 판단 근거 | `docs/rationale/oss-model-selection-rationale.md` |
| EXAONE LICENSE 분석 | `docs/rationale/exaone-license-extract.md` |
| SDD v2 | `docs/architecture/oss-model-evaluation-design.md` |
| 3+1 합의 보고서 | `docs/review/consensus-002-oss-model-evaluation.md` |

## 3+1 합의 결과

**판정**: REQUEST_CHANGES (Agent B 채택) → SDD v2 작성으로 해소

- Agent A (구현): APPROVE_WITH_CHANGES — HIGH 4 (GB10 segfault, format+Parser 충돌, EXAONE Modelfile 토큰, judge용 LlmClient)
- Agent B (품질/안전성): REQUEST_CHANGES — HIGH 6 (보안, 통계, judge 편향, 라이센스, 데이터 주권, sanitization)
- Agent C (대안): APPROVE_WITH_CHANGES — STRONG 3 (promptfoo, Claude 베이스라인, 한국어 judge 보정)

합의 결과 총 HIGH 12 + MED 6 + LOW 5 + 누락 6 = **29개 변경** 반영 → SDD v2 생성.

## 코드 구현 (feature/oss-model-eval 브랜치)

### 커밋 3개

```
c1c046a docs(eval): OSS 모델 자체 호스팅 평가 SDD v2 + 합의 + 판단 근거
c4bd454 refactor(shared): extractOracleTokens를 공용 유틸로 export (단계 0)
9373de7 chore(eval): docker-compose 평가 인프라 + sanity check 통과 (단계 1)
```

### 단계 0: extractOracleTokens 공용 유틸 export (TDD)

- `packages/shared/src/utils/oracle-tokens.ts` 신규 — ORACLE_TOKEN_REGEX + extractOracleTokens
- `packages/shared/src/utils/oracle-tokens.test.ts` 신규 — 13 케이스 (RED → GREEN → REFACTOR)
- `scope-validator.service.ts` + `seed.data.test.ts` — import로 전환, 로컬 중복 제거
- 전체: 11 파일 / 83 케이스 GREEN

### 단계 1: docker-compose 평가 인프라 + sanity check

**docker-compose.yml 추가 (7개 서비스)**:
- ollama (자체 호스팅 추론)
- langfuse-db (postgres)
- langfuse-clickhouse (분석 DB, named volume으로 권한 문제 해결)
- langfuse-redis (BullMQ redis와 분리)
- langfuse-minio (blob)
- langfuse-worker
- langfuse (web UI)

**sanity check 결과**:
```
PASS  M2 EXAONE 3.5 32B       (41s)
PASS  M3 Qwen3-Coder-Next     (37s)
PASS  M4 Qwen2.5-Coder 32B    (14s)
PASS  M5 Llama 3.3 70B        (52s)
PASS: 4 / FAIL: 0
```

모든 모델 C7(60s) 이내. GB10 Issue #15318(DGX Spark segfault) 위험 해소.

**트러블슈팅 기록**:
- sanity-check.sh: docker compose exec에 container name → service name 수정 + sudo 환경 cd fix
- Langfuse v3: CLICKHOUSE_URL 미설정 restart loop → 풀 인프라(5컨테이너) 도입
- ClickHouse: host dir 권한 문제(user 101:101 vs 1000:1000) → named volume으로 해결

## 다음 세션 우선순위

1. **단계 2**: EXAONE 4.0 32B GGUF 수동 import — HuggingFace 다운로드 + Modelfile(대괄호 토큰 `[|system|]` 검증) + `ollama create` + sanity
2. **단계 3**: `LlmClientFactory` 신설 + `ChatOllama` provider 분기 + `format: 'json'` vs `StructuredOutputParser` 실증 (TDD)
3. **단계 4**: Gold Set A(30) + Gold Set B(30, Claude 합성 → 사용자 검수)
4. **단계 5~7**: promptfoo 설정 + assertion 7개 + eval.controller
5. **단계 8~9**: Phase 0 Claude 베이스라인 → R1~R4 평가 라운드 실행
6. **단계 10**: ADR-010 + 운영 모델 교체
