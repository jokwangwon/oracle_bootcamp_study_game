# 세션 로그 — 2026-04-14

## 작업 요약

OSS 평가 단계 8-run 결과 수집 + **전체 FAIL 원인 분석 + P0 하네스 수정 + SDD/ADR 공식화**.

세션 시작 시점: `nohup` 백그라운드 평가(PID 376254)는 2026-04-13 13:25 KST에
완료되어 있었음. `apps/eval-results/` 하위 5개 모델 라운드 존재.

## 1. Phase 1 — 데이터 기반 원인 분석

### 평가 결과 수집 (5개 모델, 각 60 testcase)

| 모델 | MT3 (≥90%) | MT4 (≥95%) | 판정 |
|------|------------|------------|------|
| M1 EXAONE 4.0 32B | 83.3% | 76.7% | FAIL (최고 근접) |
| M2 EXAONE 3.5 32B | 81.4% | 64.4% | FAIL |
| M3 Qwen3-Coder-Next 80B | 63.3% | 78.3% | FAIL |
| M4 Qwen2.5-Coder 32B | 46.7% | 83.3% | FAIL |
| M5 Llama 3.3 70B | — | — | runner crash × 60 |

### 실패 토큰 집계 (M1~M4, 240 testcase)

- **MT3 위반 TOP**: `SQL`×24(term-match description의 메타용어), `MANAGER`×12,
  `CLERK`×6, `SALESMAN`×6 (대부분 `WHERE JOB = 'MANAGER'` 형태의 **문자열 리터럴 내부**)
- **MT4 위반 TOP**: `10`×6, `>`×5, `COUNT(*)`×4, `20`×3, `SYSDATE`×2 등
  (숫자 리터럴/연산자/집계 표현 — 빈칸 정답으로 자연스러움)
- **MT4 blank 개수 불일치**: 13건 (모델 instruction-following 실패)
- **4모델 전원 공통 실패 testcase 6개**: 모두 화이트리스트 설계 결함과 직접 충돌

### 근본 원인 분류

1. **`extractOracleTokens` 토크나이저가 식별자와 데이터 리터럴 혼동** (Agent B 발견)
   — `'MANAGER'` 같은 문자열 리터럴 내부가 MT3 위반으로 오탐
2. **MT4가 `blanks[i].answer`를 화이트리스트로 검증** — MT3와 중복, 자연스러운 리터럴 정답 불허
3. **`week1-sql-basics.scope.ts` 31개 키워드만 등록** — JOB 값/DNAME/SQL 메타용어/alias 누락
4. **모델 instruction-following 약점** — MT4 blank 개수 불일치 13건
5. **M5 Llama 3.3 runner OOM 의심** — num_ctx 조정 필요

결론: 실패의 ~80%는 **평가 하네스(가이드 + 센서)의 결함**, 모델 성능이 아님.

## 2. Phase 2 — 3+1 합의 프로토콜 (CLAUDE.md §3)

"SDD 명세 검토 + 아키텍처 의사결정" 해당. Agent A/B/C 병렬 독립 분석 + Reviewer 합성.

### 핵심 기여
- **Agent A (구현 분석가)**: 옵션별 수정 파일/LOC/회귀 위험 정량화. 옵션 4↔5 충돌 지적
- **Agent B (품질 검증가)**: 토크나이저 근본 버그 발견, SDD §3.1 수정 필수, seed↔DB migration 동반 필요
- **Agent C (대안 탐색가)**: Ollama schema-constrained decoding 강력 추천, Gold Set이 Claude 산출물이라는 메타 편향 지적

### Reviewer 합의 결과

| 옵션 | 결정 | 근거 |
|------|------|------|
| 1 화이트리스트 확장 | **조건부 채택** | 토크나이저 수정 동반 |
| 2 MT4 answer 검증 제거 | **채택** | 3/3 consensus |
| 3 합격선 완화 | **기각** | 3/3 (결함 은폐 + SDD 한계 초과) |
| 4 프롬프트 constraint | **부분 채택** | constraint만, few-shot 제외 |
| 5 M5 재실행 | **채택(후행)** | digest pin 갱신 |
| C 대안: schema-constrained decoding | **P2 실험 트랙 분리** | |
| C 대안: Jaccard 점수화 / Gold Set 메타 편향 | **SDD v3 이연** | |

## 3. P0 코드 수정 (CLAUDE.md §1 위반 → 복구)

**주의**: 작업 초기에 코드부터 먼저 커밋(b1a1c84)한 뒤, 세션 후반에 사용자 지적으로
SDD §1 "문서 우선" 원칙 위반을 인식. 즉시 SDD/ADR 후행 작성으로 순서 회복.

### 수정 4건 (커밋 `b1a1c84`)

1. **`packages/shared/src/utils/oracle-tokens.ts`**:
   문자열 리터럴(`'...'`, `"..."`) 내부를 토큰 추출에서 제외 (정규식 전처리).
   `ScopeValidatorService`/MT3 assertion 동시 혜택.
2. **`apps/api/src/modules/ai/eval/assertions/blank-consistency.ts`**:
   MT4에서 `blanks[i].answer` 화이트리스트 검증 블록(L75-92) 제거.
   빈칸 정답은 리터럴/연산자 허용.
3. **`apps/api/src/modules/content/seed/data/week1-sql-basics.scope.ts`**:
   JOB 5개(MANAGER/CLERK/SALESMAN/ANALYST/PRESIDENT) + DNAME 4개(ACCOUNTING/
   RESEARCH/SALES/OPERATIONS) + "SQL" + alias 6개 추가.
4. **`apps/api/src/modules/ai/prompts/blank-typing.prompt.ts`**:
   규칙 9 — "`___` 개수와 blanks 배열 길이 정확 일치" constraint 추가.

## 4. SDD/ADR/migration 공식화 (커밋 `3197f8d`)

1. **`docs/architecture/oss-model-evaluation-design.md`** §3.1 MT3/MT4 정의 갱신
   + 변경 이력 v2.7 patch 추가
2. **`docs/decisions/ADR-010-oss-eval-harness-fix.md`** 신규 — 3+1 합의 공식 기록
3. **`SeedService.syncScopes()`** 추가 — `scope.ts` 기준 `weekly_scope` 멱등 UPSERT.
   TypeORM 공식 migration 체계 없는 현 구조에서 divergence 방지.

## 5. 이번 세션 커밋 6건 (feature/oss-model-eval)

```
3197f8d docs(eval): SDD v2.7 + ADR-010 + weekly_scope 동기화 메서드
b1a1c84 fix(eval): OSS 평가 전체 FAIL 근본 원인 4건 수정 (P0)
d6dee82 docs(eval): OSS 5개 모델 평가 결과 + 전체 FAIL 원인 분석 rationale
4074951 chore(gitignore): apps/eval-results/ 경로 추가
45dfca1 feat(eval): standalone 평가 스크립트 + 5개 모델 배치 실행 (단계 8)
32a0e5c feat(eval): EvalModule — 관리자 전용 평가 트리거 엔드포인트 (단계 7)
```

## 6. 산출물

| 파일 | 용도 |
|------|------|
| `docs/rationale/oss-eval-failure-analysis-2026-04-14.md` | 3+1 합의 원문 + 근거 데이터 |
| `docs/decisions/ADR-010-oss-eval-harness-fix.md` | 공식 의사결정 |
| `apps/api/src/modules/content/seed/seed.service.sync-scopes.test.ts` | migration 테스트 3건 |
| 기타 assertion/tokenizer/scope/prompt 테스트 신규 4건 | 회귀 방어 |

## 7. 테스트 / 회귀

- API 모듈 전수: **291/291 GREEN** (기존 287 + 신규 4)
- shared 토크나이저: **16/16 GREEN** (기존 13 + 신규 3)
- typecheck OK

## 8. 다음 세션 (이어받기)

1. **`SeedService.syncScopes()` 호출** — 개발 DB에서 scope 동기화
   (API 재기동 시 자동 호출되지 않음 — 관리자 트리거 또는 별도 스크립트 필요 시 추가)
2. **P1 재평가 (R2 라운드)** — 수정된 하네스로 5모델 재측정
   - M1~M4: `run-all-models.sh` 재실행 (~3h)
   - M5: `num_ctx=4096, num_predict=1024` 별도 재실행 (~1.5h)
   - **Modelfile digest pin 갱신** (Agent B 지적, ADR-010 주의사항)
3. **결과 판정**: R2가 여전히 FAIL이면 ADR-010-A 후속 논의 (schema-constrained
   decoding 파일럿 또는 SDD v3 합격선 재설계)
4. **P2 실험 트랙**: M4 단일로 Ollama `format: <json-schema>` 파일럿
5. **Gold Set 메타 편향** (Claude 산출) — SDD v3 장기 과제로 보관

## 9. 주요 의사결정

### 1. SDD-first 원칙 회복 (CLAUDE.md §1)
P0 코드를 먼저 커밋(b1a1c84)한 뒤 사용자 지적으로 SDD §3.1/ADR-010을 후행 작성.
향후 동일 패턴 반복하지 않도록 기록. 큰 결정 시 docs/rationale/ 작성 규칙은
세션 전반에 적용됨.

### 2. Migration 전략 — SeedService 메서드 추가
TypeORM 공식 migration 체계 신설은 범위 초과로 판단. `SeedService.syncScopes()`가
`scope.ts`를 단일 source of truth로 삼아 멱등 UPSERT. 필요 시 관리자 엔드포인트나
부트 시 호출 추가 가능.

### 3. Gold Set 메타 편향은 범위 밖으로 보류
Agent C 지적(Gold Set이 Claude 산출 → OSS 평가는 "Claude 어휘 재현력" 측정)은
SDD v3 장기 과제로 기록. 현 ADR-010은 하네스 결함 수정에 집중.

## 10. 환경/운영 메모

- `apps/eval-results/` 경로를 `.gitignore`에 추가 (재현 가능 산출물)
- `.claude/settings.local.json`은 로컬 파일로 미커밋 유지
- **재평가 전 필수**: `SeedService.syncScopes()` 또는 DB 재시드로 weekly_scope 동기화
- M5 재실행 시 `Modelfile` 수정 시 GGUF digest pin 갱신 (SDD MED 테이블)
