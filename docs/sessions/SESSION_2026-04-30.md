# 세션 로그 — 2026-04-30 Session 16

## 한줄 요약

**§4 v3 사전 정렬 (노션 사용자 직접 정리 입력) + 노션 시안 페이지 생성 + PR-13 코드 PR (#63 — 통합 e2e 하네스, 결함 8건 발견·fix)** — 단위 테스트 1284 GREEN 인데 e2e 첫 가동 즉시 결함 8건 노출 (Session 14 hotfix #1/#2 정확한 재현 2건 포함). PR-13 의의 즉시 입증.

---

## 1. 세션 개요

| 항목 | 값 |
|---|---|
| 트리거 | Session 15 다음 작업 0순위 = PR-13 코드 PR (Phase 2, 1주 선행 머지) |
| 합의 가동 | 0 (사전 정렬만) |
| 사용자 결정 | 다수 (옵션 선택 흐름) |
| 신규 docs | 1 (본 세션 로그) |
| 신규 memory | 2 (`project_ai_pipeline_v3_direction.md` + `feedback_model_decoupling_test_first.md`) |
| 신규 PR | 1 (PR #63) |
| 노션 페이지 신규 | 1 (`352b362f-1ade-817c-95be-d9ad3e7d2774` — 시스템 입력용 V1) |
| 코드 변화 | 결함 fix 5건 (운영 코드) + e2e 인프라 신규 |
| 외부 노트북 검증 | 0 (본 PR 머지 후 사용자 작업 — 1주 buffer) |

---

## 2. 트랙 1 — §4 v3 의논 (사전 정렬, 합의 미가동)

### 2.1 컨텍스트 변화

PR #62 (Session 15 docs) 머지 직후 사용자가 의논 트리거 — "시스템과 문제 퀄리티 개선 방향".

핵심 발견: 사용자가 부트캠프 운영자로서 매일 직접 정리한 노션 데이터를 시스템 입력으로 사용 의도. 기존 SDD §4 v2 의 "노션 raw 강의자료 → LLM 정리" 가정과 다름.

### 2.2 사용자 결정 (사전 정렬)

| 항목 | 결정 |
|---|---|
| 입력 데이터 | 사용자 직접 매일 정리 (수업 + 실습 + 트러블슈팅) |
| Stage 2 (LLM 정리) | **폐기 방향** (옵션 B 마크다운 파서) — **획일화 우려 사용자 통찰** |
| Stage 3 (범위 추론) | 사용자 메타 (Notion property) 직접 사용, 자동 추론 폐기 |
| Few-shot 출처 | 트러블슈팅 데이터 1순위 |
| PII | 합성 데이터 (scott/hr) 강제 |
| 모델 분리 | 잠정 보류 (M3 단일 + prompt 만 분리) |
| 노션 템플릿 v0.1 | 8 properties + 4 섹션 + 트러블슈팅 6필드 (변경 가능) |
| Mini/Full | Full 단일 (품질 우선) |
| 합의 가동 시점 | 노션 **1일치** 작성 후 (1주일 → 1일치 단축) |
| 기수 분리 | **새 DB** (기존 "일지 목록" DB 와 분리) |
| 코드 설계 정책 | **모델 부 분리 + 가상 데이터로 언제든 테스트 가능** (영구 feedback memory) |

### 2.3 영향 매핑 (15건)

ADR-007 패턴으로 §4 v3 가 기존 시스템에 미치는 영향 식별. 즉시 가능 작업 PR-A/PR-B (NOTION_SYNC_CRON 일일 / ORA 토큰 카테고리), PR-13 시 함께 (#12 노션 fixture / #15 MSW mock), ADR-021 시 함께 (#8 study/resource mirror), §4 v3 합의 후 (스키마 / few-shot / prompt 분기), MVP-D 이후 (gold-set 확장).

### 2.4 노션 시안 페이지 생성

**Notion MCP 로 사용자 workspace 분석 후 직접 생성**.

| 발견 | 내용 |
|---|---|
| 메인 부트캠프 페이지 | "오라클 부트캠프 94기(아이티윌)" (`327b362f-1ade-80b9-a039-cf8fb01cae6e`) |
| 기존 "일지 목록" DB | 32일차까지 누적 (사용자 학습 노트, 부트캠프 강의 정리) |
| 기존 일일 노트 형식 | 주제별 자유 구조 (`### Tablespace` / `### Data file` 등). 4섹션 강제 X |

→ 사용자 결정: **새 DB 생성**, 기존 일지 목록과 분리. 시스템 입력 전용.

**생성된 페이지**: `352b362f-1ade-817c-95be-d9ad3e7d2774` — "🎮 게임 입력 일지 (시스템 입력용 V1)"

페이지 본문:
1. 📌 목적 안내 callout
2. DB 변환 안내 (Turn into → Database)
3. Properties 명세 8개
4. Page Body 4섹션 헤더 + 트러블슈팅 6필드 sub-template
5. 빈 템플릿 (toggle)
6. 작성 예시 W1D1 (Oracle 환경 연결 + SELECT 기초, 트러블슈팅 2건)
7. 시스템 측 파이프라인 참고 (Stage 1~5)
8. 사용 안내 + PII 정책

**사용자 진행 중**: 노션 1일차 작성. 완료 후 §4 v3 합의 가동.

---

## 3. 트랙 2 — PR-13 코드 PR (#63)

### 3.1 SDD 검토

PR-13 SDD `docs/architecture/integrated-e2e-harness-design.md` (Session 15 작성). 6 단계, ~2700 LOC, ~4d.

기존 untracked 파일 발견 (Phase 1A 부분 작성):
- `apps/api/test/e2e-setup.ts`
- `apps/api/test/setup-env.ts`
- `apps/api/test/.env.test`
- `apps/api/vitest.config.e2e.ts`

### 3.2 진행 순서 — Phase 1 → Phase 3 → Phase 2 → 검증

SDD 단계 번호 와 다른 순서 (인프라 우선). Phase 4 (Playwright smoke) / Phase 5 (회귀 매트릭스 명시) / Phase 6 (CI workflow) 는 follow-up 으로 이월.

### 3.3 Phase 1 — e2e 부트스트랩 + helpers

| 작업 | 결과 |
|---|---|
| Phase 1A 검증 (e2e-setup / setup-env / .env.test / vitest.config.e2e) | ✅ |
| Phase 1B helpers 신규 (env-check / login-helper / seed-helper) | ✅ |
| e2e-setup.ts 보강 (`assertTestEnvLoaded` 호출) | ✅ |
| package.json `test:e2e` script 추가 | ✅ |
| typecheck | PASS |

### 3.4 Phase 3 — apps/e2e 패키지 + Playwright + docker-compose

| 작업 | 결과 |
|---|---|
| `apps/e2e/package.json` (Playwright + MSW devDeps) | ✅ |
| `apps/e2e/playwright.config.ts` (aarch64 호환) | ✅ |
| `apps/e2e/docker-compose.test.yml` (test-db 5435 / test-redis 6380, 5종 안전장치) | ✅ |
| `apps/e2e/tsconfig.json` + `.gitignore` + `.env.test` | ✅ |
| Dockerfile 존재 확인 (apps/api / apps/web) | ✅ 둘 다 존재 |

**포트 변경**: `5433` 점유 (다른 프로젝트 `ai-bench-db`) → `5435` 로 변경.

### 3.5 Phase 2 — api e2e tests (auth + discussion + migration)

| 파일 | LOC | 핵심 회귀 |
|---|---|---|
| `auth.e2e.test.ts` | ~140 | hotfix #2 jsonwebtoken expiresIn / dual-mode / logout revoke |
| `migration-runner.e2e.test.ts` | ~70 | hotfix #1 EXTRACT IMMUTABLE expression index |
| `discussion.e2e.test.ts` | ~130 | hotfix #3 contract / #5 Throttler / #6 vote shape / #7 self-vote |

**총 ~340 LOC**. typecheck PASS. login-helper 시그니처 정정 1건 (loginUser 의 첫 인자 username → email — `auth.controller.ts:119` 가 email 사용).

### 3.6 e2e 직접 실행 — 결함 8건 발견·fix

사용자 sudo 비밀번호 임시 공유 → docker compose up + e2e 실행. **단위 테스트 1284 GREEN 인데 실 부팅에서 결함 8건 노출**:

| # | 결함 | 원인 | fix |
|---|---|---|---|
| **#12** | GameModeRegistry constructor DI undefined | vitest esbuild 가 `emitDecoratorMetadata` 미지원 (esbuild#257) | `unplugin-swc` + `@swc/core` 설치, vitest.config.e2e SWC plugin |
| **#13** | typeorm `tryToRequire` Missing initializer | dynamic `require('.ts file')` → Node 미인식 | `ts-node` 설치, setup-env 에서 `register/transpile-only` |
| **#14** | DiscussionService.opts DI 실패 | `opts: {} = {}` 인자 → `Function<Object>` inject 시도 (Session 14 hotfix #1 패턴 잔존) | `@Optional()` decorator |
| **#15** | typeorm migrations glob `*.{ts,js}` 가 `*.test.ts` 매칭 | vitest cjs/esm 충돌 | `discoverMigrationFiles()` 필터 (timestamp + test/spec 제외) |
| **#16** | `synchronize: true` + `migrationsRun: true` 충돌 (빈 DB ALTER 마이그레이션 fail) | typeorm 부팅 순서 또는 entity autoLoad 미작동 | `NODE_ENV=test` 분기 — e2e 는 sync 만, 마이그레이션 별도 |
| **#17** | Throttler 가 e2e 다수 register 차단 | register 한도 1h 3회, 같은 IP | `ThrottlerModule.skipIf` + `.env.test THROTTLE_DISABLED=1` |
| **#18** | RefreshTokenService payload `exp` + 글로벌 `signOptions.expiresIn` merge → register 즉시 500 | NestJS JwtService 가 글로벌 + 인자 merge. payload 에 exp 있는데 expiresIn 도 들어감 | `jsonwebtoken` 직접 import (JwtService 우회). **Session 14 hotfix #2 정확한 재현** |
| **#19** | `last_activity_at::timestamptz` 의 `EXTRACT(EPOCH FROM ...)` STABLE → 1714000013000 expression index 거부 | PostgreSQL 16: timestamptz 에 EXTRACT EPOCH = STABLE (timezone 변환 의존). 운영 마이그레이션 자체 결함 | follow-up PR (1714000014000 — timestamp cast). 본 PR 에선 it.skip + reason. **Session 14 hotfix #1 정확한 재현** |

추가 미해결 (it.skip):
- **#20** auth.e2e refresh (cookie) — supertest cookie jar 미세 이슈. body 경로는 PASS. follow-up.

### 3.7 최종 검증

| 검증 | 결과 |
|---|---|
| typecheck | PASS |
| 단위 테스트 회귀 | **1284 passed | 1 skipped** — 회귀 0 |
| e2e | **16 passed | 2 skipped** (#19 / #20) |

### 3.8 PR 생성

**PR #63**: https://github.com/jokwangwon/oracle_bootcamp_study_game/pull/63

- 브랜치: `feature/pr-13-integrated-e2e-harness`
- Pre-commit hook 의 turbo 가 apps/e2e test 실행 시도 → Playwright/docker 의존이라 fail. apps/e2e/package.json 의 `test` 를 noop 으로 변경 (실 실행은 `test:e2e`).

---

## 4. 트랙 3 — 메모리 / 노션 시안 / 영향 매핑

### 4.1 신규 메모리 2건

- `project_ai_pipeline_v3_direction.md` — §4 v3 방향성 (사용자 직접 정리 입력 / Stage 2 폐기 / 획일화 우려 / 노션 템플릿 v0.1 시안)
- `feedback_model_decoupling_test_first.md` — 영구 feedback (모델 부 분리 + 가상 데이터 / PR-13 정합)

### 4.2 노션 페이지 시안 v0.1

`352b362f-1ade-817c-95be-d9ad3e7d2774` — "🎮 게임 입력 일지 (시스템 입력용 V1)"

사용자가 **노션 1일차 작성 중** (본 세션 종료 시점).

---

## 5. 안 한 것 (다음 세션)

- **사용자 노션 1일차 작성 완료** — 트리거
- **§4 v3 합의 가동** — Stage 1.5 / 3.5 / few-shot / 부트캠프 범위 확장 / 노션 스키마 확장 / 누락된 날 cron / PII 정책 묶음
- **PR #63 머지** — 외부 노트북 라이브 검증 buffer 1주 권장
- **#19 follow-up** — 1714000014000 신규 마이그레이션 (timestamp cast 또는 IMMUTABLE 함수)
- **#20 follow-up** — supertest agent 패턴 (cookie jar 자동 보존)
- **PR-13 Phase 4** — Playwright smoke (auth-flow / discussion-flow)
- **PR-13 Phase 5** — hotfix 11건 회귀 매트릭스 명시 검증
- **PR-13 Phase 6** — `.github/workflows/e2e.yml` CI 통합
- **ADR-021 코드 PR 4분할** — PR #63 머지 + 1주 안정화 후 진입

---

## 6. 본 세션 마무리

본 SESSION 로그 + CONTEXT.md 갱신 + 메모리 갱신 — 별도 docs PR 또는 다음 세션 첫 작업.

**머지 순서 권장**:
1. PR #63 (PR-13 코드) — 1주 안정화
2. 본 docs (Session 16) — 별도 docs PR 또는 PR #63 에 추가 commit
3. §4 v3 합의 — 노션 1일치 후
4. ADR-021 코드 PR 4분할 — PR #63 머지 + 1주 후

---

## 7. 다음 세션 첫 행동 권장

**0순위 — 사용자 노션 1일치 작성 완료 확인 + §4 v3 합의 가동**

- 노션 페이지 (`352b362f...`) 의 작성 데이터 확인
- 합의 의제 묶음:
  - Stage 1.5 Oracle 공식 레퍼런스 크롤링 (라이선스 / 신선도 / 크롤 전략)
  - Stage 3.5 노션 ↔ 레퍼런스 교차 정확도 점수
  - Stage 5 ⑥ 레퍼런스 교차 검증 단계
  - notion_documents 스키마 확장 (sections JSONB / day INT / ora_errors TEXT[])
  - 누락된 날 cron fallback (NOTION_SYNC_CRON 일일 + 누락 알림)
  - 부트캠프 범위 확장 (SQL 한정 → PL/SQL/Admin/Backup/Tuning 전반)
  - few-shot 예시 출처 (트러블슈팅 데이터 1순위)
  - 모델 분리 (정리/생성 — 잠정 보류, MVP-D 이후)
  - 노션 템플릿 v0.1 → v0.2 (사용자 작성 경험 반영)

**1순위 — PR #63 머지 (사용자 작업)** — 외부 노트북 라이브 검증 1주 buffer

**2순위 — #19 follow-up 마이그레이션 PR (작은, 0.5d)**

**3순위 — ADR-021 코드 PR 4분할 (PR #63 머지 + 1주 후)**

- PR-A 마이그레이션 (1d): virtual root question 시드 + category + role + audit_log + reports + moderation 컬럼
- PR-B API + RBAC (2d): RolesGuard + community endpoint 23종 + 학생 글 수정 코드 강제
- PR-C UI (2d): `/community/*` 라우트 + 비대칭 4-카드 (Community 카드 신규)
- PR-D 모더레이션 (1.5d): 신고 + queue + lock/pin/hide/restore + audit log

---

## 8. 본 세션 요약 메트릭

| 항목 | 값 |
|---|---|
| 합의 가동 | 0 (사전 정렬만) |
| 사용자 결정 | 12+ (의논·옵션 선택 흐름) |
| 노션 페이지 신규 | 1 |
| PR 신규 | 1 (PR #63) |
| 발견·fix 결함 | 8건 (#12~#19) |
| Session 14 hotfix 정확한 재현 | 2건 (#18 / #19) |
| 신규 코드 LOC (PR #63) | ~723 (commit stat) |
| 신규 docs (본) | 1 (본 세션 로그) |
| 신규 메모리 | 2 |
| 단위 테스트 회귀 | 0 (1284 passed | 1 skipped) |
| e2e 결과 | 16 passed | 2 skipped (18) |
