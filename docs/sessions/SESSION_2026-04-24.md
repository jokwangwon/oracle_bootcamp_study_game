# 세션 로그 — 2026-04-24

## 한줄 요약

**PR #14~#17 전수 머지 · 695 → 779 + 1 skipped (+84 tests)**. Session 6 PR #2 (free-form 채점 배선 7 커밋) 머지 → 3+1 사후 검증으로 CRITICAL-1 발견 → hotfix PR #15 → D3 Hybrid 대칭성 보강 PR #16 → Session 7 시작 (SM-2 SR) Phase 0 + Phase 1 완료 + ADR-019 + PR-1 entity/migration 머지.

---

## 1. PR #14 — Session 6 PR #2 (consensus-007 free-form 채점 배선)

**브랜치**: `feature/mvp-b-session6-pr2-free-form-wiring` → **머지 `6b8ee7f`**
**테스트**: 695 → **751 + 1 skipped (+57)**

consensus-007 §5 실행 계획 7 커밋 순차 TDD:

| # | 커밋 | 범위 |
|---|------|------|
| C2-1 | `8049ed4` | `LlmClient.invoke(messages, opts?: { metadata?: WhitelistedMetadata })` + `PlayerAnswer.sessionId` + Orchestrator→Layer3Grader sessionId 전파 |
| C2-2 | `3dc5b6d` | `GradingMeasurementService` 신규 + `OpsEventKind 'grading_measured'` + `GradingMeasuredPayload` (MT6/MT8 집계 입력) |
| C2-3 | `b3c7532` | `GradingModule` AppModule 등록 (격리 해제) — 정적 소스 grep smoke |
| C2-4 | `548cbd2` | `GameSessionService.gradeFreeForm()` private 추출 + `ENABLE_FREE_FORM_GRADING` kill-switch + `ScopeValidatorService.getAllowlist` |
| C2-5 | `3c5ff2f` | `LLM_JUDGE_TIMEOUT_MS=8000` + `withLlmJudgeTimeout` + `LlmJudgeTimeoutError` + HTTP 503 + held persist |
| C2-6 | `e60a857` | `answer_history` 3 신규 컬럼 (`rationale`/`sanitization_flags`/`ast_failure_reason`) + migration 1714000005000 + 7항 persist + `user_token_hash_epoch` |
| C2-7 | `3a56c6e` | `langfuse-payload-privacy` helper + E2E `OLLAMA_INTEGRATION` gate |

상태: `ENABLE_FREE_FORM_GRADING=false` 기본 유지 — 프로덕션 활성은 수동 전환 (사용자 Q2).

---

## 2. 3+1 합의 프로토콜 사후 검증 (consensus-007 4 판단)

PR #14 머지 직후 사용자 요청으로 **구현 중 내린 4개 세부 판단** 을 3+1 로 사후 검증.

### 2.1 검증 대상

- 판단 1 (C2-2): `grading_measured` 이벤트를 `ops_event_log` 에 저장
- 판단 2 (C2-5): `LlmJudgeTimeoutError` throw surface + held persist + HTTP 503
- 판단 3 (C2-6): `ALTER TABLE ADD COLUMN IF NOT EXISTS` + synchronize 병행
- 판단 4 (C2-7): `langfuse-payload-privacy` 를 `grading/graders/` 에 배치

### 2.2 Agent 결과 + Reviewer 합의

- **합의율 25%** (1/4 완전 일치) — 구현 세부 검증 정상 범위
- **Session 4 격상 CRITICAL 1건** (Agent B 단독 지적):
  - `persistHeldAnswer` 가 `userTokenHash/Epoch/graderDigest/gradingLayersUsed/rationale/sanitizationFlags/astFailureReason` 전부 누락 + `hintsUsed` 출처 불일치 + held persist 실패 시 ops_event_log 강제 기록 없음 → **WORM 영구 감사 불구 위험**
- HIGH 2건:
  - Agent B: 16 hex (hashUserToken 출력) 미탐지 — 주석과 구현 모순
  - Agent C: `langfuse-payload-privacy` 를 `ai/` 로 이동 조기 변경

Reviewer 가 코드 라인 축자 확인 후 모두 격상/채택.

### 2.3 PR #15 hotfix 머지 (`d24b0aa`)

**브랜치**: `fix/pr2-held-persist-audit` → 단일 커밋 `b8334e9`
**테스트**: 751 → **754 + 1 skipped (+3)**

- CRITICAL-1: `persistHeldAnswer` 재설계 → `buildAnswerHistoryInput` 재사용 패턴. `LlmJudgeTimeoutError.sanitizationFlags` 첨부 경로 (Orchestrator 가 catch → re-throw). `recordHeldPersistFail` 최후 방어선
- CRITICAL-2: `langfuse-payload-privacy` → `ai/` 이동 (git mv) + `hex-16` boundary lookaround 패턴 추가
- H3: `GradingOrchestrator.grade()` throw 계약 JSDoc 명시
- H4: `docs/architecture/multi-agent-system-design.md` §7.3/7.4 신설 — 합의율 패턴 관찰 기록 (설계 40~70% vs 구현 세부 ~25%), Reviewer 직접 사실 검증 책임 명시

### 2.4 PR #16 — D3 Hybrid 대칭성 (Q2=b 이행)

**브랜치**: `feat/ops-event-log-user-token-hash` → 단일 커밋 `fa149af` → **머지 `fda3ba9`**
**테스트**: 754 → **764 + 1 skipped (+10)**

- `OpsEventLogEntity` 에 `user_token_hash varchar(32)` + `user_token_hash_epoch smallint` nullable 컬럼
- migration `1714000006000-AddOpsEventLogUserTokenHash`
- `GradingMeasurementService.resolveHashAndEpoch` private 헬퍼 (fail-safe null)
- `measureGrading` / `recordLlmTimeout` / `recordHeldPersistFail` 3 메서드 모두 저장 직전에 resolve
- 운영 규약: **분석 집계 쿼리는 `user_token_hash` 사용**, `user_id` 평문은 관리자 직접 조회용 FK 로만

---

## 3. Session 7 시작 — SM-2 Spaced Repetition

### 3.1 Phase 0 자동화 검토 질문지

**파일**: `docs/rationale/sm2-spaced-repetition-brief-v1.md` (신규)

- 배경: `answer_history` 엔티티 주석에서 SR 존재 계약 + consensus-004 에서 SM-2 방향성 + ADR-014 결합
- ADR 번호 정정: consensus-004 "ADR-018 SM-2" → **ADR-019** (ADR-018 은 salt rotation 선점)
- 레퍼런스: SM-2 / FSRS / Leitner 비교 — **SM-2 채택 지배적**
- 요구사항 후보 R1~R8
- Phase 0 질문지 5건 (C1~C3 + D1~D2)

**사용자 답변**:
| # | 결정 |
|---|------|
| C1 | (d) 망각 방지 + 학습 효율 + 지속 동기 |
| C2 | (a) R1~R6 — 단 D1=(d) 와 모순으로 R6 기각 → 최종 R1~R5 |
| C3 | (b) 복습 완료율 ≥ 70% |
| D1 | (d) foundation 게이트 없음, 1회 답변부터 즉시 SR 편입 |
| D2 | (a) SR 먼저 + 소진 후 random `min(N, M)` |

### 3.2 Phase 1 — 3+1 합의 (Agent A/B/C + Reviewer)

**파일**: `docs/review/consensus-008-sm2-spaced-repetition.md` (신규, 합의 보고서 원문)

- **Agent A (구현)**: 조건부 채택. pure function 분리 + entity 스키마 상세 + migration 1714000007000 + `DataSource.transaction` 권고. **A-CRITICAL 3건**: gameMode 필터 누락 / 게스트 null / WORM 복제 금지.
- **Agent B (안전)**: **1인 지적 CRITICAL 4건 격상**:
  - B-C1: `user_token_hash` + `_epoch` 컬럼 필수 (D3 Hybrid 대칭)
  - B-C2: UPSERT 원자성 (race 방어)
  - B-C3: `held` / `admin-override` 분기 (held 편입 스킵 / override overwrite)
  - B-C4: 일일 신규 편입 상한 + `ops_event_log(sr_queue_overflow)`
- **Agent C (대안)**: MVP 80% 채택. 조기 수정 2건: C3 복합 지표 + D2 SR 상한 70%. quality 공식 조합식 제안.

Reviewer 판정:
- 합의율 **36.8%** (구현 세부 검증 정상 범위)
- **CRITICAL 전원 반영** (B-C4 만 기본값 50→100 완화)
- 사용자 C3/D2 결정 덮어쓰기 → 사용자 재확인 Q1/Q2

### 3.3 사용자 Q1~Q5 결정

| Q | 결정 |
|---|------|
| Q1 | 권장 — **C3 복합 지표** (primary retention + secondary 완료율 + guard quality 저하) |
| Q2 | 권장 — **D2 SR 상한 70%** (`ceil(M × 0.7)`) |
| Q3 | (b) SM-2-lite ease clamp [2.3, 2.6] **항상 ON** |
| Q4 | (a) 일일 신규 편입 상한 **100/user/day** |
| Q5 | (a) quality 공식에서 **timePenalty 제거** (서버 재계산 부채 분리) |

**PR-0 조사 결과**: `answer.submittedAt` 을 `timeTakenMs` 로 직접 저장 — 서버 재계산 경로 부재. Agent B HIGH 지적 사실 확인. Q5=(a) 로 timePenalty 제거 + ADR-019 §8 기술 부채로 분리.

### 3.4 ADR-019 작성

**파일**: `docs/decisions/ADR-019-sm2-spaced-repetition.md` (신규)

- §3.1 `sm2Next` canonical 공식 + SM-2-lite clamp 항상 ON
- §3.2 `mapAnswerToQuality` 최종 공식 (`base - hintPenalty`, timePenalty 제거)
- §4 스키마 + **§4.1 파생/캐시 테이블 성격 명문화** (WORM 미적용)
- §5 `submitAnswer` Tx1/Tx2 분리 + fail-open 전략
- §6 복합 성공 지표
- §7 제외/연기 (review-only UI / FSRS / Redis 캐시 / foundation 게이트 폐기)
- §8 기술 부채 (timeTakenMs 서버 재계산 + race advisory lock)
- §9 PR-1~6 로드맵

### 3.5 PR #17 — PR-1 entity + migration (`254cd45`)

**브랜치**: `feat/sm2-pr1-entity-migration` → 2 커밋 (`a3aa5cc` + `ff943af` INDEX 보강)
**테스트**: 764 → **779 + 1 skipped (+15)**

- `apps/api/src/modules/review/entities/review-queue.entity.ts` — 복합 PK + SM-2 상태 7 컬럼 + `algorithm_version` + D3 Hybrid 대칭 컬럼 + partial index
- `apps/api/src/migrations/1714000007000-AddReviewQueue.ts` — `CREATE TABLE IF NOT EXISTS` + `CREATE INDEX` + down 역순 (IF EXISTS)
- `apps/api/src/modules/review/review.module.ts` — PR-1 은 entity 만 등록 (service/controller 는 PR-2~PR-4)
- `apps/api/src/app.module.ts` — `ReviewModule` imports 추가
- 문서 3종 (ADR-019 + consensus-008 + 브리프 §8 업데이트)
- INDEX 의 rationale 섹션에 SM-2 브리프 등록

TDD +15:
- entity metadata 7건 (테이블명/복합PK/SM-2 상태/algorithm_version/D3 Hybrid/partial index/auto timestamp)
- migration SQL 6건 (name/up create/up 컬럼 전수/up 인덱스 WHERE/down 역순/up-down 대칭)

---

## 4. 누적 PR 목록

| PR | 범위 | 커밋 | 머지 | 테스트 변화 |
|----|------|-----|------|----------|
| #14 | Session 6 PR #2 — free-form 채점 배선 (C2-1~C2-7) | 7 | `6b8ee7f` | 695 → 751+1s |
| #15 | 3+1 사후 검증 hotfix (CRITICAL-1 + HIGH 2) | 1 (`b8334e9`) | `d24b0aa` | 751 → 754+1s |
| #16 | ops_event_log D3 Hybrid 대칭성 | 1 (`fa149af`) | `fda3ba9` | 754 → 764+1s |
| #17 | Session 7 PR-1 — SM-2 entity + migration + ADR-019 | 2 | `254cd45` | 764 → **779+1s** |

**오늘 세션 총 누적**: 11 커밋 / **+84 tests** / typecheck clean / pre-commit 전 커밋 자동 통과.

---

## 5. 문서 신규/업데이트

| 문서 | 상태 |
|------|------|
| `docs/decisions/ADR-019-sm2-spaced-repetition.md` | 신규 |
| `docs/review/consensus-008-sm2-spaced-repetition.md` | 신규 |
| `docs/rationale/sm2-spaced-repetition-brief-v1.md` | 신규 |
| `docs/architecture/multi-agent-system-design.md` §7.3/7.4 | 추가 |
| `docs/INDEX.md` | rationale 섹션 확장 |

---

## 6. 남은 대기 항목

### 6.1 Session 7 Phase 2 — PR-2~6 순차 구현

| PR | 범위 |
|----|------|
| PR-2 | `sm2Next` + `mapAnswerToQuality` pure function + TDD |
| PR-3 | `submitAnswer` Tx1/Tx2 분리 + `ReviewQueueService.upsertAfterAnswer` + `overwriteAfterOverride` + 일일 상한 |
| PR-4 | `pickQuestions(..., srRatio=0.7)` + `GET /api/solo/review-queue` + `/solo/start` 통합 |
| PR-5 | Next.js 세션 헤더 "오늘 복습 N" 뱃지 UI |
| PR-6 | `sr_metrics_daily` 집계 쿼리 (primary/secondary/guard) |

### 6.2 consensus-007 사후 검증 후속 (별개)

- **MVP-C**: `AbortController` + LangChain `RunnableConfig.signal` 도입 (Ollama HTTP cancel, GPU 누수 방어)
- **Session 8+ 기술 부채**:
  - `Round.startedAt` + 서버 측 timeTakenMs 재계산 + 프론트 epoch ms 전송 — Q5=(a) 해소 PR
  - CI schema drift check (Layer 4)
  - WORM 트리거 설치 시 dev `synchronize=false` 전환

### 6.3 활성화 대기

- **`ENABLE_FREE_FORM_GRADING=false`** 프로덕션 기본 유지 — 수동 전환 결정 보류
- **PR #14 실 활성 관측 미실행** — staging 부팅 smoke 후 사용자 결정 필요

---

## 7. 세션 운영 메모

- **3+1 합의율 패턴 확인**: 설계 합의 (consensus-008 36.8%) vs 구현 세부 검증 (consensus-007 사후 25%) 두 사례 축적. `multi-agent-system-design.md` §7.3 관찰 기록 첫 참고 사례.
- **1인 지적 CRITICAL 격상 룰 2회 발동**: 둘 다 Agent B 의 "WORM/PII 감사 체인" 관점. Session 4 룰이 보안 쏠림인지 아니면 진짜 위험 감지인지 판정은 Reviewer 의 **코드 축자 확인** 으로 방어.
- **Phase 0 → Phase 1 → ADR 공정 풀 사이클**: Session 3 (consensus-005) 이후 가장 체계적 실행. 브리프 + 질문지 + 3+1 합의 + 사용자 Q + ADR + consensus 원문 보존 + 단계적 PR 로드맵 연결.
- **Claude Code root 실행 문제**: 세션 종료 시 `chown -R delangi:delangi apps docs` 필요.

---

## 8. 환경 상태 확인 사항

- **docker compose** 상태 미확인 (이 세션에서 실 테스트 실행 안 함)
- **staging/prod 배포** 미실행. PR #14~#17 모두 main 머지 후 실 배포는 사용자 작업.
- **`.env`** 변경 사항 (추가된 env):
  - `ENABLE_FREE_FORM_GRADING=false` (PR #14)
  - `LLM_JUDGE_TIMEOUT_MS=8000` (PR #14)
  - `SR_DAILY_NEW_CAP=100` (PR-3 구현 후)
  - `SR_EASE_CLAMP` 관련 — ADR-019 Q3=b (항상 ON, env flag 는 optional)

---

**다음 세션 시작 시**: Session 7 PR-2 (`sm2Next` + `mapAnswerToQuality` pure function TDD) 착수 권장. 또는 사용자 결정에 따라 PR #14 활성 게이트 (staging smoke + `ENABLE_FREE_FORM_GRADING=true` 전환 검토).
