# 세션 로그 — 2026-04-24

## 한줄 요약

**PR #14~#26 (13 PR) · 695 → 936 + 1 skipped (+241 tests) · 3+1 합의 2건 완료 (consensus-008 SM-2 / consensus-009 UX 재설계) · ADR-019 구현 완결 + ADR-020 초안 확정**. 오전: Session 6 PR #2 free-form 채점 배선 + 3+1 사후 검증 hotfix + D3 대칭. 오후: ADR-019 SM-2 Spaced Repetition PR-1~6 순차 구현. 저녁: 오답 노트(블로그 사이드바 UX v2) + UX 재설계 Phase 1 완결 (consensus-009 합의율 63% / Agent B 단독 CRITICAL 5건 전원 격상 채택).

---

## 1. PR #14 — Session 6 PR #2 (consensus-007 free-form 채점 배선)

**브랜치**: `feature/mvp-b-session6-pr2-free-form-wiring` → **머지 `6b8ee7f`**
**테스트**: 695 → **751 + 1 skipped (+57)**

consensus-007 §5 실행 계획 7 커밋 순차 TDD:

| # | 커밋 | 범위 |
|---|------|------|
| C2-1 | `8049ed4` | `LlmClient.invoke(messages, opts?: { metadata?: WhitelistedMetadata })` + `PlayerAnswer.sessionId` + Orchestrator→Layer3Grader sessionId 전파 |
| C2-2 | `3dc5b6d` | `GradingMeasurementService` 신규 + `OpsEventKind 'grading_measured'` + `GradingMeasuredPayload` (MT6/MT8 집계 입력) |
| C2-3 | `b3c7532` | `GradingModule` AppModule 등록 (격리 해제) — 정적 소스 grep smoke |
| C2-4 | `548cbd2` | `GameSessionService.gradeFreeForm()` private 추출 + `ENABLE_FREE_FORM_GRADING` kill-switch + `ScopeValidatorService.getAllowlist` |
| C2-5 | `3c5ff2f` | `LLM_JUDGE_TIMEOUT_MS=8000` + `withLlmJudgeTimeout` + `LlmJudgeTimeoutError` + HTTP 503 + held persist |
| C2-6 | `e60a857` | `answer_history` 3 신규 컬럼 (`rationale`/`sanitization_flags`/`ast_failure_reason`) + migration 1714000005000 + 7항 persist + `user_token_hash_epoch` |
| C2-7 | `3a56c6e` | `langfuse-payload-privacy` helper + E2E `OLLAMA_INTEGRATION` gate |

상태: `ENABLE_FREE_FORM_GRADING=false` 기본 유지 — 프로덕션 활성은 수동 전환 (사용자 Q2).

---

## 2. 3+1 합의 프로토콜 사후 검증 (consensus-007 4 판단)

PR #14 머지 직후 사용자 요청으로 **구현 중 내린 4개 세부 판단** 을 3+1 로 사후 검증.

### 2.1 검증 대상

- 판단 1 (C2-2): `grading_measured` 이벤트를 `ops_event_log` 에 저장
- 판단 2 (C2-5): `LlmJudgeTimeoutError` throw surface + held persist + HTTP 503
- 판단 3 (C2-6): `ALTER TABLE ADD COLUMN IF NOT EXISTS` + synchronize 병행
- 판단 4 (C2-7): `langfuse-payload-privacy` 를 `grading/graders/` 에 배치

### 2.2 Agent 결과 + Reviewer 합의

- **합의율 25%** (1/4 완전 일치) — 구현 세부 검증 정상 범위
- **Session 4 격상 CRITICAL 1건** (Agent B 단독 지적):
  - `persistHeldAnswer` 가 `userTokenHash/Epoch/graderDigest/gradingLayersUsed/rationale/sanitizationFlags/astFailureReason` 전부 누락 + `hintsUsed` 출처 불일치 + held persist 실패 시 ops_event_log 강제 기록 없음 → **WORM 영구 감사 불구 위험**
- HIGH 2건:
  - Agent B: 16 hex (hashUserToken 출력) 미탐지 — 주석과 구현 모순
  - Agent C: `langfuse-payload-privacy` 를 `ai/` 로 이동 조기 변경

Reviewer 가 코드 라인 축자 확인 후 모두 격상/채택.

### 2.3 PR #15 hotfix 머지 (`d24b0aa`)

**브랜치**: `fix/pr2-held-persist-audit` → 단일 커밋 `b8334e9`
**테스트**: 751 → **754 + 1 skipped (+3)**

- CRITICAL-1: `persistHeldAnswer` 재설계 → `buildAnswerHistoryInput` 재사용 패턴. `LlmJudgeTimeoutError.sanitizationFlags` 첨부 경로 (Orchestrator 가 catch → re-throw). `recordHeldPersistFail` 최후 방어선
- CRITICAL-2: `langfuse-payload-privacy` → `ai/` 이동 (git mv) + `hex-16` boundary lookaround 패턴 추가
- H3: `GradingOrchestrator.grade()` throw 계약 JSDoc 명시
- H4: `docs/architecture/multi-agent-system-design.md` §7.3/7.4 신설 — 합의율 패턴 관찰 기록 (설계 40~70% vs 구현 세부 ~25%), Reviewer 직접 사실 검증 책임 명시

### 2.4 PR #16 — D3 Hybrid 대칭성 (Q2=b 이행)

**브랜치**: `feat/ops-event-log-user-token-hash` → 단일 커밋 `fa149af` → **머지 `fda3ba9`**
**테스트**: 754 → **764 + 1 skipped (+10)**

- `OpsEventLogEntity` 에 `user_token_hash varchar(32)` + `user_token_hash_epoch smallint` nullable 컬럼
- migration `1714000006000-AddOpsEventLogUserTokenHash`
- `GradingMeasurementService.resolveHashAndEpoch` private 헬퍼 (fail-safe null)
- `measureGrading` / `recordLlmTimeout` / `recordHeldPersistFail` 3 메서드 모두 저장 직전에 resolve
- 운영 규약: **분석 집계 쿼리는 `user_token_hash` 사용**, `user_id` 평문은 관리자 직접 조회용 FK 로만

---

## 3. Session 7 시작 — SM-2 Spaced Repetition

### 3.1 Phase 0 자동화 검토 질문지

**파일**: `docs/rationale/sm2-spaced-repetition-brief-v1.md` (신규)

- 배경: `answer_history` 엔티티 주석에서 SR 존재 계약 + consensus-004 에서 SM-2 방향성 + ADR-014 결합
- ADR 번호 정정: consensus-004 "ADR-018 SM-2" → **ADR-019** (ADR-018 은 salt rotation 선점)
- 레퍼런스: SM-2 / FSRS / Leitner 비교 — **SM-2 채택 지배적**
- 요구사항 후보 R1~R8
- Phase 0 질문지 5건 (C1~C3 + D1~D2)

**사용자 답변**:
| # | 결정 |
|---|------|
| C1 | (d) 망각 방지 + 학습 효율 + 지속 동기 |
| C2 | (a) R1~R6 — 단 D1=(d) 와 모순으로 R6 기각 → 최종 R1~R5 |
| C3 | (b) 복습 완료율 ≥ 70% |
| D1 | (d) foundation 게이트 없음, 1회 답변부터 즉시 SR 편입 |
| D2 | (a) SR 먼저 + 소진 후 random `min(N, M)` |

### 3.2 Phase 1 — 3+1 합의 (Agent A/B/C + Reviewer)

**파일**: `docs/review/consensus-008-sm2-spaced-repetition.md` (신규, 합의 보고서 원문)

- **Agent A (구현)**: 조건부 채택. pure function 분리 + entity 스키마 상세 + migration 1714000007000 + `DataSource.transaction` 권고. **A-CRITICAL 3건**: gameMode 필터 누락 / 게스트 null / WORM 복제 금지.
- **Agent B (안전)**: **1인 지적 CRITICAL 4건 격상**:
  - B-C1: `user_token_hash` + `_epoch` 컬럼 필수 (D3 Hybrid 대칭)
  - B-C2: UPSERT 원자성 (race 방어)
  - B-C3: `held` / `admin-override` 분기 (held 편입 스킵 / override overwrite)
  - B-C4: 일일 신규 편입 상한 + `ops_event_log(sr_queue_overflow)`
- **Agent C (대안)**: MVP 80% 채택. 조기 수정 2건: C3 복합 지표 + D2 SR 상한 70%. quality 공식 조합식 제안.

Reviewer 판정:
- 합의율 **36.8%** (구현 세부 검증 정상 범위)
- **CRITICAL 전원 반영** (B-C4 만 기본값 50→100 완화)
- 사용자 C3/D2 결정 덮어쓰기 → 사용자 재확인 Q1/Q2

### 3.3 사용자 Q1~Q5 결정

| Q | 결정 |
|---|------|
| Q1 | 권장 — **C3 복합 지표** (primary retention + secondary 완료율 + guard quality 저하) |
| Q2 | 권장 — **D2 SR 상한 70%** (`ceil(M × 0.7)`) |
| Q3 | (b) SM-2-lite ease clamp [2.3, 2.6] **항상 ON** |
| Q4 | (a) 일일 신규 편입 상한 **100/user/day** |
| Q5 | (a) quality 공식에서 **timePenalty 제거** (서버 재계산 부채 분리) |

**PR-0 조사 결과**: `answer.submittedAt` 을 `timeTakenMs` 로 직접 저장 — 서버 재계산 경로 부재. Agent B HIGH 지적 사실 확인. Q5=(a) 로 timePenalty 제거 + ADR-019 §8 기술 부채로 분리.

### 3.4 ADR-019 작성

**파일**: `docs/decisions/ADR-019-sm2-spaced-repetition.md` (신규)

- §3.1 `sm2Next` canonical 공식 + SM-2-lite clamp 항상 ON
- §3.2 `mapAnswerToQuality` 최종 공식 (`base - hintPenalty`, timePenalty 제거)
- §4 스키마 + **§4.1 파생/캐시 테이블 성격 명문화** (WORM 미적용)
- §5 `submitAnswer` Tx1/Tx2 분리 + fail-open 전략
- §6 복합 성공 지표
- §7 제외/연기 (review-only UI / FSRS / Redis 캐시 / foundation 게이트 폐기)
- §8 기술 부채 (timeTakenMs 서버 재계산 + race advisory lock)
- §9 PR-1~6 로드맵

### 3.5 PR #17 — PR-1 entity + migration (`254cd45`)

**브랜치**: `feat/sm2-pr1-entity-migration` → 2 커밋 (`a3aa5cc` + `ff943af` INDEX 보강)
**테스트**: 764 → **779 + 1 skipped (+15)**

- `apps/api/src/modules/review/entities/review-queue.entity.ts` — 복합 PK + SM-2 상태 7 컬럼 + `algorithm_version` + D3 Hybrid 대칭 컬럼 + partial index
- `apps/api/src/migrations/1714000007000-AddReviewQueue.ts` — `CREATE TABLE IF NOT EXISTS` + `CREATE INDEX` + down 역순 (IF EXISTS)
- `apps/api/src/modules/review/review.module.ts` — PR-1 은 entity 만 등록 (service/controller 는 PR-2~PR-4)
- `apps/api/src/app.module.ts` — `ReviewModule` imports 추가
- 문서 3종 (ADR-019 + consensus-008 + 브리프 §8 업데이트)
- INDEX 의 rationale 섹션에 SM-2 브리프 등록

TDD +15:
- entity metadata 7건 (테이블명/복합PK/SM-2 상태/algorithm_version/D3 Hybrid/partial index/auto timestamp)
- migration SQL 6건 (name/up create/up 컬럼 전수/up 인덱스 WHERE/down 역순/up-down 대칭)

---

## 3.6 PR-2 — sm2Next + mapAnswerToQuality pure function (`3bb71a4`, PR #19)

**브랜치**: `feature/sm2-pr2-pure-functions` → **머지 `7807731`**
**테스트**: 779 → **821 + 1 skipped (+42)**

ADR-019 §3.1 / §3.2 순수 함수. 외부 의존 0, caller 가 `anchor` 주입, prev/anchor 불변.

- `sm2Next(prev, quality, anchor?)` — canonical SM-2 + SM-2-lite clamp [2.3, 2.6] 항상 ON (Q3=b).
- `mapAnswerToQuality({ isCorrect, partialScore, hintsUsed, gradingMethod })` — `base - hintPenalty`. timePenalty 제거 (Q5=a). `gradingMethod='held'` → null.

단일 파일 `apps/api/src/modules/review/sm2.ts` 55 LOC + 42 tests (경계값 완비).

---

## 3.7 PR-3 — ReviewQueueService + submitAnswer Tx2 (`2151f7b`, PR #20)

**브랜치**: `feature/sm2-pr3-review-queue-service` → 머지
**테스트**: 821 → **855 + 1 skipped (+34)**

ADR-019 §5.1 / §5.3. PR-2 순수 함수 소비 + WORM/fail-open 정책.

- `ReviewQueueService.upsertAfterAnswer(userId, questionId, quality, anchor?)` — existing 없을 때 `SR_DAILY_NEW_CAP`(=100, Q4=a) 체크 → 초과 drop + `sr_queue_overflow`. 통과 시 `INSERT ... ON CONFLICT DO UPDATE` (race 방어 B-C2).
- `ReviewQueueService.overwriteAfterOverride(...)` — admin-override. cap/existing 체크 없이 덮어쓰기 (B-C3).
- `easeFactor` numeric(4,3) → `toFixed(3)` 문자열. D3 Hybrid hash/epoch fail-safe resolve.
- `GameSessionService.routeToReviewQueue` (private) — Tx2 fail-open 배선. held/guest/service 미주입 → no-op (회귀 0). 예외는 `recordSrUpsertFail` 기록 후 흡수.
- `OpsEventKind` 확장: `sr_queue_overflow`, `sr_upsert_failed` + `GradingMeasurementService` 2 메서드.
- `env.validation` — `SR_DAILY_NEW_CAP` (기본 100, 양의 정수 검증). `.env.example` 업데이트.

신규 테스트 +34 (ReviewQueueService 18 + grading-measurement 9 + game-session 8 — 회귀 0).

---

## 3.8 PR-4 — /solo/start SR 혼합 + GET /review-queue (`0996536`, PR #21)

**브랜치**: `feature/sm2-pr4-solo-start-integration` → 머지
**테스트**: 855 → **880 + 1 skipped (+25)**

ADR-019 §5.2. `srRatio=0.7` 혼합 편성 + 세션 뱃지 입력 API.

- `ReviewQueueService.findDue(userId, criteria, limit, now?)` — 2-step: review_queue user-scoped + due_at ≤ now → questions JOIN (topic/week/gameMode/difficulty). due_at ASC 순서 보존.
- `ReviewQueueService.countDueForUser(userId, now?)` — 전체 범위 due 건수 (PR-5 소비).
- `QuestionPoolService.pickRandom(..., opts?: { excludeIds })` — `q.id NOT IN` 필터. 미전달/빈 배열 시 기존 쿼리 (회귀 0).
- `GameSessionService.pickQuestionsForSolo` (private) — `srLimit=ceil(rounds*0.7)` due 우선 + 부족분은 `excludeIds` 로 중복 방지된 random. userId/ReviewQueueService 미주입 → 기존 random-only (레거시 호환).
- `GameSessionService.getReviewQueueSummary(userId)` — `{ dueCount }`. 서비스 미주입 시 0.
- `GameController.start` — JWT `user.sub` 를 `StartSoloInput.userId` 로 전파. `GameController.reviewQueue` — `GET /games/solo/review-queue`.
- `ReviewModule` — `forFeature` 에 QuestionEntity 추가 (JOIN 용).

신규 테스트 +25 (question-pool 5 / review-queue.service +11 / game-session +6 / game.controller 3 — 회귀 0).

---

## 3.9 PR-5 — Next.js "오늘 복습 N" 뱃지 UI (`49ec20a`, PR #22)

**브랜치**: `feature/sm2-pr5-review-badge-ui` → 머지
**테스트**: API 880+1s 유지 (web-only 변경). web typecheck clean.

ADR-019 §5.2 UI 매핑. PR-4 API 소비.

- `apiClient.solo.reviewQueue(token)` — `{ dueCount: number }` 반환 (`ReviewQueueSummary`).
- `components/ReviewBadge.tsx` — 세션 설정 화면 우측 상단 뱃지.
  - 로딩 중 / 에러 silent (SR 조회 실패가 솔로 시작 막지 않음 — Tx2 fail-open 정신).
  - `dueCount === 0` 도 표시 (SR 활성 가시화, D1=d 정신).
  - `dueCount > 0` accent 강조 스타일.
  - `aria-label` 접근성.
- `/play/solo` config 단계 상단 flex 레이아웃.

---

## 3.10 PR-6 — sr_metrics_daily 집계 + 3지표 breach (`d7a2ac8`, PR #23) — ADR-019 완결

**브랜치**: `feature/sm2-pr6-sr-metrics-daily` → 머지
**테스트**: 880 → **908 + 1 skipped (+28)** / 77 → 79 files

ADR-019 §6. 운영 3대 지표 일별 집계 + threshold breach.

- `sr_metrics_daily` 테이블 + migration 1714000008000 (metric_date DATE PK 멱등).
- `SrMetricsDailyEntity` — 3 지표 × (수치 NUMERIC(4,3) + sample_size INT) + created_at.
- `SrMetricsService`:
  - `computeMetrics(asOf?)` — 3 QueryBuilder (retention / completion / guard).
  - `snapshotDaily(asOf?)` — `upsert(['metricDate'])` 멱등 + breach 이벤트 emit.
  - `@Cron(EVERY_DAY_AT_MIDNIGHT)` `runDaily()` fail-safe.
- `evaluateSrMetricBreaches` pure function — threshold 판정 (커스텀 threshold 주입 가능).
- `OpsEventKind` 확장: `sr_metric_breach` + `SrMetricBreachPayload`.

| 지표 | 정의 | 목표 |
|---|---|---|
| Primary retention | AVG(last_quality) where repetition≥2 AND last_reviewed_at ≥ asOf−7d | ≥ 3.5 |
| Secondary completion | completed/scheduled (상한 1.0 clamp) | ≥ 0.7 |
| Guard low_rate | (last_quality ≤ 1)/total | < 0.2 |

신규 테스트 +28 (migration SQL 5 / sr-metrics.service 22 — pure·DB·cron fail-safe 커버).

---

## 3.11 저녁 — 오답 노트 + UX v2 블로그 사이드바 (PR #25)

**브랜치**: `feature/mistakes-review` → OPEN
**테스트**: 908 → **936 + 1 skipped (+28)** / 79 → 81 files

v1 (상단 드롭다운) → v2 (좌측 사이드바 블로그 스타일) 두 커밋으로 진화:

- **v1 (`13a14d0`)** — 사용자 Q1~Q3 답변 + 학습 범위 확장 대응
  - `UserMistakesService.getMistakes` 3-step (stats GROUP BY + questions 상세 + latest DISTINCT ON)
  - `MistakeSummary.byWeek/byTopic/byGameMode` 필터 무관 전체 인벤토리 → 드롭다운 동적 생성 (사용자 지적 반영)
  - `GET /api/users/me/mistakes` 엔드포인트 (JwtAuthGuard + DTO 검증)
  - Q1=b 집계 / Q2=a SR 분리 / Q3=b 정답 처리 뱃지
  - 헤더 "오답 노트" 링크 추가
  - 테스트 +19
- **v2 (`a28190e`)** — 블로그 좌측 사이드바 UI 재설계
  - 검색 (300ms debounce) + 상태/주차/주제/게임모드 섹션 + 카운트 뱃지
  - 우측: 정렬 dropdown 4종 (최근/자주틀림/주차/주제) + 활성 필터 chips + 페이지네이션
  - 백엔드 `search` ILIKE 5 컬럼 + `sort` 4종 + `status` 3종
  - `MistakeSummary.byStatus { unresolved, resolved }` 추가
  - 서비스 전략 TS-side 필터·정렬·페이지네이션 전환 (하드캡 500)
  - globals.css `<1024px` 사이드바 상단 스택 (Q8=a desktop-first)
  - 테스트 +9 (user-mistakes.service 16 → 25)

**docker 재빌드 + staging 실 배포 smoke 완료** (GET /api/users/me/mistakes 401, web /review/mistakes 200).

---

## 3.12 저녁 — UX 재설계 Phase 1 완결 (ADR-020 초안 + consensus-009, PR #26)

**브랜치**: `docs/adr-020-ux-redesign` → OPEN

**3+1 합의 (consensus-009)**:
- Agent A (구현) / B (품질) / C (대안) 독립 병렬 분석
- Reviewer 교차 비교 + **Agent B 단독 CRITICAL 5건 전원 격상 채택** (Session 4 룰, 코드 라인 축자 검증 완료)
- **합의율 63.0%** (완전 37.0% + 부분 25.9%) — consensus-008 (37%) 대비 개선

**Agent B 단독 CRITICAL 5건** (축자 검증 완료):
- C-B1: Stored XSS sanitize + CSP/helmet 부재 (`main.ts` 확인)
- C-B2: JWT in localStorage × 토론 (`auth-storage.ts:11` 확인)
- C-B3: auth 엔드포인트 rate limit 부재 (`auth.module.ts` 확인)
- C-B4: vote UNIQUE + self-vote + race 방어
- C-B5: pre-commit Layer 3-a3 경로 하드코딩 (`.githooks/pre-commit:44` 확인)

**사용자 Q10~Q15 확정**:
| # | 답변 | 결정 |
|---|------|------|
| Q10 | a | R5 완전형 (upvote + downvote) |
| Q11 | a | R4 + httpOnly 쿠키 동일 PR 번들 |
| Q12 | a | desktop only (mobile 깨짐 허용) |
| Q13 | a | 공개 가입 (화이트리스트 없음) |
| Q14 | 4 | Tailwind + shadcn/ui 유지 |
| Q15 | b | light 기본 (programmers 지향) |

**ADR-020 주요 결정**:
- UI: Tailwind CSS v3.4 + shadcn/ui + Radix + next-themes (`defaultTheme="light"`)
- R4 스키마: 3-테이블 (threads/posts/votes) + 1-level nested + soft delete + Reddit hot 공식
- R6 voting: PK UNIQUE + ON CONFLICT + DB CHECK self-vote 방지 트리거
- 마이그레이션: 컴포넌트별 + Strangler Fig 혼합
- **12 PR 로드맵** (약 13일) — PR-1~7 (인프라 + 보안 기반) 선행, PR-8~9 UI 이전, PR-10~12 R4+R6+UI
- Q11=a 로 PR-6+PR-11 통합 → 총 12 PR (원래 13)

**문서**:
- `docs/decisions/ADR-020-ux-redesign.md` (신규, 12 섹션)
- `docs/review/consensus-009-ux-redesign.md` (신규, 10 섹션)

---

## 4. 누적 PR 목록

| PR | 범위 | 커밋 | 머지 | 테스트 변화 |
|----|------|-----|------|----------|
| #14 | Session 6 PR #2 — free-form 채점 배선 (C2-1~C2-7) | 7 | `6b8ee7f` | 695 → 751+1s |
| #15 | 3+1 사후 검증 hotfix (CRITICAL-1 + HIGH 2) | 1 (`b8334e9`) | `d24b0aa` | 751 → 754+1s |
| #16 | ops_event_log D3 Hybrid 대칭성 | 1 (`fa149af`) | `fda3ba9` | 754 → 764+1s |
| #17 | Session 7 PR-1 — SM-2 entity + migration + ADR-019 | 2 | `254cd45` | 764 → 779+1s |
| #18 | Session 7 세션 로그 초안 (docs) | 1 (`5a14d06`) | `c4974ed` | 변화 없음 |
| #19 | Session 7 PR-2 — sm2Next + mapAnswerToQuality pure fn | 1 (`3bb71a4`) | `7807731` | 779 → 821+1s |
| #20 | Session 7 PR-3 — ReviewQueueService + submitAnswer Tx2 | 1 (`2151f7b`) | `292a0ba` | 821 → 855+1s |
| #21 | Session 7 PR-4 — /solo/start SR 혼합 + GET /review-queue | 1 (`0996536`) | `f5a5e3b` | 855 → 880+1s |
| #22 | Session 7 PR-5 — Next.js 뱃지 UI | 1 (`49ec20a`) | `269588f` | 880 → 880+1s (web-only) |
| #23 | Session 7 PR-6 — sr_metrics_daily 집계 + 3지표 breach | 1 (`d7a2ac8`) | `7ae0026` | 880 → 908+1s |
| #24 | 세션 로그 + CONTEXT/INDEX 갱신 (PR-2~6 반영) | 1 (`18f38be`) | `c55fce7` | 변화 없음 |
| #25 | 오답 노트 + UX v2 좌측 사이드바 블로그 스타일 | 2 (`13a14d0`+`a28190e`) | OPEN | 908 → **936+1s (+28)** |
| #26 | ADR-020 UX 재설계 초안 + consensus-009 (Phase 1 완결) | 1 (`5c5d171`) | OPEN | 변화 없음 |

**오늘 세션 총 누적**: 21 커밋 / **+241 tests (695 → 936+1s)** / typecheck clean / pre-commit 전 커밋 자동 통과 / **ADR-019 구현 완결 + 오답 노트 v2 + ADR-020 초안 확정**.

---

## 5. 문서 신규/업데이트

| 문서 | 상태 |
|------|------|
| `docs/decisions/ADR-019-sm2-spaced-repetition.md` | 신규 (오후) |
| `docs/decisions/ADR-020-ux-redesign.md` | 신규 (저녁) |
| `docs/review/consensus-008-sm2-spaced-repetition.md` | 신규 (오후) |
| `docs/review/consensus-009-ux-redesign.md` | 신규 (저녁) |
| `docs/rationale/sm2-spaced-repetition-brief-v1.md` | 신규 (오후) |
| `docs/architecture/multi-agent-system-design.md` §7.3/7.4 | 추가 (오전) |
| `docs/INDEX.md` | 2026-04-24 3행 추가 (오전/오후/저녁) |
| `docs/CONTEXT.md` | ADR-019 완결 + ADR-020 초안 반영 |
| `docs/sessions/SESSION_2026-04-24.md` | §3.6~§3.12 추가, 누적 PR 표 확장 |

---

## 6. 남은 대기 항목

### 6.1 Session 7 ADR-019 로드맵 → ✅ 완결 (PR-1~PR-6)

모든 구현 PR main 머지 + staging 재빌드/스모크 완료 (16:15 경).

- [x] staging 재빌드 — migration 1714000007000 + 1714000008000 자동 실행 확인
- [x] `/play/solo` 접속 → 뱃지 렌더 (dueCount 0) 확인
- [ ] 24h 후 `@Cron(EVERY_DAY_AT_MIDNIGHT)` 자동 실행 관측 — `sr_metrics_daily` 1 행 INSERT 확인
- [ ] 실 플레이 후 솔로 재진입 시 `/solo/start` 응답에 due 우선 편성 확인

### 6.1b ADR-020 UX 재설계 로드맵 → 🟡 초안 완료, 12 PR 대기

- PR-1 Tailwind v3.4 + postcss + utils — 다음 세션 후보 1
- PR-2 CSS 변수 → Tailwind theme 브리지 + light 팔레트
- PR-3 helmet + CSP + Next.js headers (CRITICAL-B1)
- PR-4 next-themes + ThemeProvider + Header 토글
- PR-5 shadcn/ui 초기화 + Button/Card/Dialog/Input
- PR-6 `@nestjs/throttler` + auth rate limit + `RedisRateLimiter` 일반화 (CRITICAL-B3) — **독립 병렬 가능, 보안 즉시 이득**
- PR-7 pre-commit Layer 3-a3 재귀 + pii-regression 확장 (CRITICAL-B5) — **독립**
- PR-8 Header + `/`, `/login`, `/register` Tailwind 재작성
- PR-9 `/play/solo` Tailwind 이전
- PR-10 **(Q11=a 통합)** httpOnly 쿠키 + `/auth/refresh` + CSRF + R4 discussion 스키마/API + sanitize-html (CRITICAL-B2 + C-B4 부분)
- PR-11 R6 vote UNIQUE + self-vote CHECK + race 테스트 (CRITICAL-B4)
- PR-12 discussion 페이지 + VoteButton + rehype-sanitize (CRITICAL-B1)

**다음 세션 권장 착수**: PR-6 (auth rate limit) 또는 PR-7 (pre-commit 재귀) — 독립 + 보안 즉시 이득.

### 6.1c PR #25 (오답 노트 + UX v2) → 🟡 OPEN 리뷰 대기

- 머지 확인 후 main 동기화 — UX 재설계 PR-9 (`/play/solo`) 와 레이아웃 공존 전제 (feature/mistakes-review 를 PR-9 범위로 흡수하거나 먼저 머지).

### 6.2 consensus-007 사후 검증 후속 (별개)

- **MVP-C**: `AbortController` + LangChain `RunnableConfig.signal` 도입 (Ollama HTTP cancel, GPU 누수 방어)
- **Session 8+ 기술 부채**:
  - `Round.startedAt` + 서버 측 timeTakenMs 재계산 + 프론트 epoch ms 전송 — Q5=(a) 해소 PR
  - CI schema drift check (Layer 4)
  - WORM 트리거 설치 시 dev `synchronize=false` 전환

### 6.3 활성화 대기

- **`ENABLE_FREE_FORM_GRADING=false`** 프로덕션 기본 유지 — 수동 전환 결정 보류
- **PR #14 실 활성 관측 미실행** — staging 부팅 smoke 후 사용자 결정 필요

---

## 7. 세션 운영 메모

- **3+1 합의 2회 연속 실행** (하루 안에): consensus-008 (SM-2, 37%) + consensus-009 (UX 재설계, 63%). **합의율 패턴 확장**: 설계만 (008, 37%) vs 설계+보안 통합 (009, 63%). §7.3 누적 관찰 테이블 업데이트 필요.
- **1인 지적 CRITICAL 격상 룰 3회 총 발동**: consensus-007 사후 (Agent B 1건) + consensus-008 (Agent B 4건 + Agent A 3건) + consensus-009 (Agent B 5건). Session 4 룰이 **보안 축 단독 책임** 패턴으로 안착. Reviewer 의 **코드 축자 확인** 이 격상 근거 (재분석 금지 규칙과 병립).
- **Phase 0 → Phase 1 → ADR 풀 사이클 하루 2번 실행**: SM-2 (오후) + UX (저녁). 브리프 + 질문지 + 3+1 합의 + 사용자 Q + ADR + consensus 원문 + PR 로드맵 형식 표준화.
- **학습 범위 확장 대응 패턴 확립**: 오답 노트 summary 3차원 집계 (byWeek/byTopic/byGameMode) 로 드롭다운 동적 생성. "새 주차 추가 시 코드 수정 없이 자동 노출" 원칙 확립 — 사용자 명시적 요구로 v1 에 반영.
- **v1 → v2 UX 재설계 (오답 노트)**: 상단 드롭다운 → 좌측 사이드바 블로그 스타일 전환. 이 경험이 ADR-020 전체 레이아웃 결정에 직접 반영됨 (사이드바 네비 패턴 확정).
- **Claude Code root 실행 문제**: 세션 종료 시 `chown -R delangi:delangi apps docs` 필요.

---

## 8. 환경 상태 확인 사항

- **docker compose** 상태 미확인 (이 세션에서 실 테스트 실행 안 함) — 다음 세션 시작 전 사용자 실기 테스트 예정.
- **staging/prod 배포** 미실행. PR #14~#23 모두 main 머지 후 실 배포는 사용자 작업.
- **`.env`** 변경 사항 (세션 누적, 새로 추가된 env 만):
  - `ENABLE_FREE_FORM_GRADING=false` (PR #14, 프로덕션 보수 기본)
  - `LLM_JUDGE_TIMEOUT_MS=8000` (PR #14)
  - `SR_DAILY_NEW_CAP=100` (PR #20 = PR-3)
  - `SR_EASE_CLAMP` 관련 — ADR-019 Q3=b (항상 ON, env flag 미정의 — 상수로 내장)
- **DB migration 신규** (세션 누적):
  - 1714000005000 answer_history grading meta (PR #14)
  - 1714000006000 ops_event_log user_token_hash (PR #16)
  - 1714000007000 review_queue (PR #17, PR-1)
  - 1714000008000 sr_metrics_daily (PR #23, PR-6)
- **새 API 엔드포인트**:
  - `GET /games/solo/review-queue` — dueCount (PR-4, ADR-019)
  - `GET /api/users/me/mistakes?topic&week&gameMode&search&sort&status&limit&offset` — 오답 노트 (PR #25)
- **새 cron job**:
  - `@Cron(EVERY_DAY_AT_MIDNIGHT)` `sr-metrics-daily` → `sr_metrics_daily` 스냅샷 + breach emit (ADR-019).

---

**다음 세션 시작 시**:

1. PR #25 (오답 노트) + PR #26 (ADR-020 docs) 머지 확인
2. ADR-020 12 PR 로드맵 착수 — **권장 시작**: PR-6 (auth rate limit, 독립) 또는 PR-7 (pre-commit 재귀, 독립)
3. 또는 PR-1 (Tailwind 인프라) 부터 순차 시작

**후속 유보 항목**:

ADR-019 §7:
- FSRS 전환 평가 (SM-2 데이터 3개월+ 확보 후)
- Grafana 대시보드 UI + Slack/email 알림 (operational-monitoring Phase C)
- SR_EASE_CLAMP off 재검토 (ease 변동성 6개월+ 관측 기반)
- advisory lock 재검토 (peak rps ≥ 50 도달 시, §8.2)

ADR-020 §9:
- Mobile 375px 지원 (Q12=a, 실기 이탈 3회/주 관찰 시)
- 다계정 vote bomb 방어 강화 (Q13=a, 동일 IP 3+계정 관찰 시)
- Mantine/DaisyUI/Panda 재경쟁 (Q14=4, Tailwind 유지보수 문제 시)
- 토론 LLM 요약/추천 (별도 ADR-021, pre-commit Layer 3-a3 확장 전제)

Session 8+ 기술 부채:
- `Round.startedAt` 서버 재계산 (timePenalty 재도입 전제, ADR-019 §8)
- CI schema drift check (Layer 4)
- WORM 트리거 설치 시 dev `synchronize=false` 전환
