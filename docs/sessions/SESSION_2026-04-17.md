# 세션 로그 — 2026-04-17

## 작업 요약

**MVP-A 완주 (4 커밋) + MVP-B 착수 (2 커밋) + 직전 세션 로그 백필 (1 커밋)** — 총 7 커밋.
`feature/oss-model-eval` 브랜치가 main 대비 **19 커밋 앞섬**으로 확장됨.

테스트 380 → 464 (+84), typecheck/build/부팅 전 구간 녹색 유지.

## 1. 세션 시작 — SESSION_2026-04-16.md 백필 (`202fc1c`)

직전 세션(2026-04-16) 커밋 14건이 문서화되지 않은 채 진행됨을 확인.
CLAUDE.md §4 세션 프로토콜 위반이므로 먼저 정리:

- `docs/sessions/SESSION_2026-04-16.md` 신규 작성 (182줄, 3부 구성)
  * 오후 1부: ADR-011 조건 #2/#3/#4 일괄 이행
  * 오후 2부: 단계 10 운영 개시 준비 완료
  * 야간부: v2.9 합의 (consensus-004) + 병렬 2에이전트 부팅 회귀 수정
- `docs/INDEX.md` 에 부팅 회귀 + env 노출 항목 1줄 추가
- "사용자가 Reviewer 합의를 오버라이드"한 선례(5모드 축소 → 유지) 기록 — 향후 합의 해석 근거

## 2. MVP-A 완주 (4 커밋)

### `452e9aa` feat(game): MVP-A 1단계 — 객관식 모드(Mode 6) + answerFormat 축 (ADR-012)

- `packages/shared`: `GAME_MODE_IDS`에 `'multiple-choice'` + `ANSWER_FORMATS` 타입 신설
- `MultipleChoiceContent` discriminated union 추가 (stem + options[id,text] + allowMultiple?)
- `Question.answerFormat?: AnswerFormat` optional 필드 (기존 시드/픽스처 호환)
- `QuestionEntity`에 `answer_format varchar(20) DEFAULT 'single-token'` 컬럼
- `MultipleChoiceMode` Strategy 구현 — CSV 선택 파싱, Set equality 채점, all-or-nothing (역피라미드는 MVP-B)
- **20 cases TDD** (metadata/generateRound/단일정답/복수정답/점수)
- 병행 수정: `notion.controller.test.ts` jobId 정규식 (`:` → `-`, 커밋 327578a 미반영 pre-existing 회귀)

### `6c282f2` feat(ops): MVP-A MT6/MT7/MT8 스켈레톤 (ADR-017)

- `ops_question_measurements` 에 차원 컬럼 4개 추가 (mode/answer_format/grading_method/layer_1_resolved, 모두 nullable)
- `OpsEventKind` 에 `mt6_breach` / `mt7_breach` / `mt8_breach` + `GateBreachMetric` 분리
- `evaluateGates` 확장: MT6(free-form Layer1 해소율 ≥60%), MT7(캡스톤 DAG 위반 =0), MT8(LLM-judge 호출률 ≤15%)
- `fetchAggregate` SQL에 `free_form_total` / `free_form_layer1_resolved` / `llm_judge_calls` / `graded_total` 집계 추가
- `renderWindowOneReport` 에 MT6/MT7/MT8 행 추가 (MT7은 "MVP-C 이후 활성" 표기)
- `mapBreachKind` 헬퍼 — metric별 event kind 분리
- **10 TDD cases 추가** (기존 14 + 신규 10 = 24 cases)
- **데이터 소스 자연 gating**: free-form 샘플 0이면 MT6/MT8 skip → 조용한 대기

### `1c29b3b` feat(ai): MVP-A AI 생성 객관식 분기 (ADR-012 §6)

- `multipleChoiceContentSchema` / `multipleChoiceOutputSchema` / `MULTIPLE_CHOICE_GENERATION_PROMPT` 신설
- `AiQuestionGenerator` MC 분기 추가 — 계산적 검증 3중:
  1. `options[].id` 고유성 (Set size 비교)
  2. `correctOptionIds ⊆ options[].id`
  3. `allowMultiple=false` → `correctOptionIds.length === 1`
- Scope validation 확장: MC는 stem + options[].text + explanation 검증 (answer는 option id라 제외)
- `SaveQuestionInput.answerFormat?` 추가
- **6 cases TDD** (정상/allowMultiple 복수정답/id 중복/invalid correctOptionId/단일정답 위반/화이트리스트 위반)
- Zod `discriminatedUnion` 제약 준수: `.refine()` 을 스키마에 붙이지 않고 AQG에서 수동 검증

### `a9895c4` feat(eval): MVP-A MC promptfoo 하네스 (ADR-012 §7)

- `mc-option-consistency` assertion 신설 (AQG 3중 검증과 동일 논리 재사용 — 운영/평가 일관성)
- `build-eval-prompt.ts` MC 분기 추가 (EvalGameMode 유니언 확장 + focusLine 전용 문구)
- `gold-set-mc.ts` **15 entries 초안** (EASY 5 / MEDIUM 7 / HARD 3, 나머지 15건은 후속 세션 TODO)
- `promptfoo-testcases.ts`: `PromptfooGameMode`/`PromptfooGoldSet` 타입 분리 + MC 통합 (총 testCase 60→75)
- `promptfoo.config.yaml` defaultTest.assert에 mc-option-consistency 추가
- `reports/schema.v1.ts` + `promptfoo-adapter.ts` 의 gameMode/goldSet enum 확장 (schema 하위 호환 유지)
- **16 cases TDD** (MT_MC 10 + gold-set-mc 5 + promptfoo-testcases 1)

## 3. MVP-B 착수 (2 커밋 / Roadmap 10개 중 2개)

MVP-B는 4주 분량이라 세션당 한 조각만. `project_mvp_b_plan.md` memory로 roadmap 관리 시작.

### `c38cc35` feat(grading): MVP-B 1단계 — GradingModule 스켈레톤 + AnswerSanitizer + 메타 컬럼

- `modules/grading/answer-sanitizer.ts` (ADR-016 §2) — "제거 + 경고 수집" 전략
  * 제어 문자 (TAB/LF/CR 보존) / HTML·XML 태그 (SQL 비교 연산자 보존) / 코드 블록 구분자 제거
  * Instruction 인젝션 의심 패턴 6종 → SUSPICIOUS_INPUT 플래그
  * 2048자 초과 → truncate + TRUNCATED 플래그
  * SQL 주석 `--` / `/* */` 보존 확인 (MC의 scope와 달리 text 내부 보존)
  * **26 cases TDD**
- `modules/grading/grading.types.ts` — `GradingVerdict`/`GradingMethod`/`GradingLayerPath`/`LayerVerdict`/`GradingResult`
- `answer-history.entity.ts` 에 채점 메타 4 컬럼 nullable 추가
  * grading_method / grader_digest / grading_layers_used / partial_score
  * WORM 트리거 (ADR-016 §6)는 별도 세션 격리 (DB 레벨 변경)
- **GradingModule AppModule 미등록** — 게임 흐름 회귀 위험 0, 배선은 Session 6

### `bc2ccaa` feat(grading): MVP-B 2단계 — Layer 2 KeywordCoverageGrader + GradingOrchestrator

- `graders/keyword-coverage.grader.ts` (ADR-013 §Layer 2)
  * `coverage = |T_expected ∩ T_student| / |T_expected|`
  * `partial = max(0, coverage - |T_student \ allowlist| * 0.05)`
  * 판정: ≥0.95 PASS / <0.3 FAIL / 그 외 UNKNOWN (Layer 3로 이관)
  * `extractOracleTokens` (shared) 재사용 — 단일 토크나이저 원칙
  * KEYWORD_GRADER_VERSION='keyword-v1' 상수 (감사용 graderDigest)
  * **13 cases TDD**
- `grading.orchestrator.ts`
  * 순서: sanitize → Layer 1 → (UNKNOWN 시) Layer 2 → (UNKNOWN 시) Layer 3 → held
  * `LAYER_1_GRADER` / `LAYER_3_GRADER` DI Symbol 토큰 + `@Optional()` — 후속 세션 교체 무중단
  * `DEFAULT_LAYER_1_GRADER` / `DEFAULT_LAYER_3_GRADER` UNKNOWN stub — 현재 Layer 2만 기능
  * rationale에 모든 Layer 근거 `'|'`로 연결 (감사 재구성 가능)
  * 모두 UNKNOWN → `gradingMethod='held'` (관리자 큐 이관)
  * **10 cases TDD**
- `grading.module.ts` providers/exports 에 KeywordCoverageGrader + GradingOrchestrator 추가
- AppModule 미등록 유지

## 4. 세션 말 상태

### 브랜치 / 테스트
- **브랜치**: `feature/oss-model-eval` — main 대비 **19 커밋** 앞섬 (오후 MVP 작업 기준. 저녁 하네스 점검 §8 에서 +5 추가 → **24 커밋**)
- **테스트**: 464 passed (직전 세션 말 380 → +84)
- **typecheck**: clean (apps/api + packages/shared)
- **nest build**: 성공, dist 최신
- **운영**: Ollama M3 운영 컨테이너 정상, 회귀 0

### 신규 memory 2건
- `project_realtime_priority.md` — 실시간 대전(ADR-015) 후순위 조정 (사용자 지시)
- `project_mvp_b_plan.md` — MVP-B 10개 세션 분할 roadmap

### 미커밋
- `.claude/settings.local.json` (사용자 환경 로컬)

## 5. 파급 효과 / 리스크

- **MVP-A 완주**: ADR-012 §구현 범위 1~4, §6, §7 이행. §5(Mode 1 variants)는 MVP-B §10에서 free-form 합류 시 도입
- **MVP-B 10세션 roadmap 확정**: 전체 공수 4주 → 세션 2개 완료로 약 20% 진행
- **Grading 파이프라인 API 안정화**: `LAYER_1_GRADER`/`LAYER_3_GRADER` DI 토큰 덕분에 Layer 1(AST, Session 3) / Layer 3(LLM, Session 4) 실 구현 주입 시 Orchestrator 변경 없음
- **brank 누적 이슈**: 19 커밋 → main 머지 검토 필요 시점 근접. MVP-B Session 3~4 완료 후 권장 머지 포인트
- **새 의존성 예고** (Session 3): `node-sql-parser` — Docker 이미지 재빌드 시간 증가 예상

## 6. 다음 세션 우선순위 (MVP-B Session 3)

**Layer 1 AstCanonicalGrader + `node-sql-parser` + Oracle 방언**

착수 시 고려:
1. `node-sql-parser` 의 Oracle 방언 커버리지 한계 확인 — 미지원 구문은 UNKNOWN fallback
2. 정규화 스펙 (ADR-013 §Layer 1):
   - identifier case fold (Oracle 기본 대문자)
   - alias 통일
   - 공백/주석 제거
   - 절 순서 표준화 (SELECT → FROM → WHERE → GROUP → HAVING → ORDER)
   - 비교 대상: 테이블/컬럼 집합, JOIN 타입/조건, WHERE 술어 집합, GROUP BY 집합, SELECT 프로젝션 순서 (프로젝션만 순서 민감)
3. 방언 테스트 스위트: `DUAL` / `ROWNUM` / `CONNECT BY` / `(+)` 조인 / `NVL` / `DECODE` / `LISTAGG` / `MERGE`
4. `LAYER_1_GRADER` DI 토큰에 AstCanonicalGrader 바인딩 추가 (`grading.module.ts`)
5. Orchestrator는 변경 불필요 — 기존 API 안정

병렬 유지 과제:
- gold-set-mc 16~30 확장 (수기)
- 프론트 결과 페이지에 correctAnswer/explanation 표시 UI (submitAnswer 응답은 이미 포함)
- Notion sync 수동 실증 (NOTION_DATABASE_ID 이미 설정, POST /api/notion/sync 호출만)

## 7. 참고: 세션 내 임시 결정

- **`.refine()` 을 discriminatedUnion 멤버에 붙이지 않는다** — Zod 제약. 호출 site에서 수동 검증 (AQG MC 분기에서 적용)
- **Layer 1/3 grader 교체는 DI 토큰 경유**로 설계 — 직접 inject 대신 `LAYER_1_GRADER` / `LAYER_3_GRADER` Symbol + `@Optional()` + default stub 패턴
- **`GradingModule` AppModule 미등록 원칙**을 MVP-B Session 6까지 유지 — 게임 흐름 회귀 방지

---

## 8. 하네스 점검 후속 작업 (5 커밋, 저녁)

세션 시작 직전 대화에서 사용자가 하네스 설정을 점검해 개선 항목 5개를 제시.
(항목 1: PostToolUse tsc 교체 — 직전 세션에서 완료 상태.) 이번 세션 저녁에
나머지 5개(2 / 3 / 4 / 5 + 별도 ESLint)를 순차 처리.

### 항목 2 (`dda09f0`) — Layer 2/3 pre-commit 센서 활성화

- `.githooks/pre-commit`: 민감정보/디버그 스캔 위에 **`npm run typecheck` + `npm test`** 추가.
  순수 문서(`docs/**`, `*.md/txt/rst/adoc`) 전용 커밋은 자동 스킵. 비상시 `SKIP_PRECOMMIT_HEAVY=1` 탈출구.
- `apps/web/package.json`: test 스크립트에 `--passWithNoTests` 추가 — web 테스트 부재로
  루트 `npm test` 가 영구 실패였던 상태 해소.
- **중요 발견**: `git config core.hooksPath` 가 설정되지 않아 기존 Layer 3 훅 자체가 유명무실.
  `core.hooksPath .githooks` 로컬 설정 적용.
- 검증 3 경로 (빈 스테이징 / 문서 전용 / 코드 파일) 모두 의도대로.

### 항목 3 (`235ceef`) — `.claude/settings.local.json` 212 → 48줄 트리밍

- 위험 wildcard 14종 제거: `curl *`, `node:*`, `git push:*`, `git reset *`, `xargs cat *`,
  `xargs ls:*`, `grep:*`, `nohup bash:*`, `bash -c ' *`, `sudo chown:*`, `sudo -n chown …`,
  `chown -R …`, `pkill -f …`.
- 일회성 구체 명령 ~150개 제거: 특정 psql 쿼리 변형, `echo "FOO=$?"` 계열 수십 개,
  구체 파일 경로 ls/wc/grep, 하드코딩 `DATABASE_URL=…:changeme@…` 평문 비밀번호 포함 npx tsx,
  긴 `python3 -c` / `node -e` 인라인 스크립트.
- 남긴 것: Web / Git(push·reset 제외) / GitHub CLI / npm+npx / 시스템+Docker /
  파일읽기 / `Skill(update-config)`.
- **사이드 이슈**: 이 파일은 tracked + not-gitignored 상태. 세션 중 하네스가 구체 명령을
  자동으로 재추가하는 현상 존재 → 공식 관례(개인용 gitignore)로 전환 여부는 추후 별도 결정.

### 항목 4 (`e62560e`) — 3+1 서브에이전트 정의 4종 (+256 LoC)

`.claude/agents/` 신설. **프롬프트 규약이었던 "3+1 합의"를 Claude Code 서브에이전트
하네스로 승격**. `multi-agent-system-design.md` §2~4 의 역할·입력·출력·프로세스를
그대로 에이전트 정의로 매핑.

- `agent-a-impl` — 구현 분석가 ("실제로 동작하는가?")
- `agent-b-quality` — 품질/안전성 검증가 ("안전하고 견고한가?")
- `agent-c-alt` — 대안 탐색가 ("더 나은 방법이 있는가?")
- `reviewer` — 합의 검토자 (교차 비교 + 최종 판정)

각 에이전트 프롬프트에 "다른 에이전트 출력 참조 금지"를 명시해 독립 분석(편향 방지)을
강제. 출력 형식은 설계 문서 §4 그대로. 모델은 모두 `inherit` — 부모 세션 모델 계승.

### 항목 5 (`964fcdc`) — Notion MCP 권한 3단 정책화

`.claude/settings.json` `permissions` 블록에 Notion MCP 14개 툴을 위험도별 3단 분류:

- **allow** (5, 읽기): `fetch`, `search`, `get-comments/teams/users`
- **ask** (7, 특정 객체 쓰기): `create-comment/pages`, `duplicate-page`,
  `update-page`, `move-pages`, `create-view`, `update-view`
- **deny** (2, 구조 변경): `create-database`, `update-data-source`

근거 + 채택하지 않은 대안 3종("전부 ask" / "전부 allow" / "deny 최소화") + 후속 액션
(리뷰 주기 · deny 해제 기록 · 감사 로그 연계)을
`docs/rationale/notion-mcp-permission-policy.md` (140줄) narrative 로 보존.

### 별도 (`f64c887`) — ESLint 뼈대 복구 (변동성 대비)

기존 `"lint": "eslint src --ext .ts"` 스크립트는 config·의존성 부재로 처음부터 동작 불가
상태. pre-commit 에 lint 센서를 **붙이지는 않되**, 필요해지면 즉시 활성화할 수 있도록
최소 뼈대만 추가.

- devDep (apps/api, packages/shared): `eslint@^8.57.0` (web 과 메이저 통일 — 8.x legacy
  config) + `@typescript-eslint/parser@^7.18.0` + `@typescript-eslint/eslint-plugin@^7.18.0`
- `.eslintrc.cjs` × 2: `eslint:recommended` + `@typescript-eslint/recommended`.
  `no-unused-vars` / `no-explicit-any` / `no-empty-function` / `no-empty-interface` 는
  `warn` 으로 완화 — 기존 코드와 급격한 충돌 방지.
- 검증: `npm run lint` 통과 (turbo 4/4). api 9 warnings (모두 warn 규칙), shared 0, web 0.
  errors 0. **pre-commit 정책 변경 없음** — Layer 2 는 typecheck + test 유지.

### 하네스 점검 말 누적 상태

- **브랜치**: `feature/oss-model-eval` — main 대비 **24 커밋** 앞섬 (+5)
- **테스트**: 464 passed (변화 없음)
- **typecheck**: clean
- **하네스 Layer 실체화 요약**:
  - Layer 1 PostToolUse `tsc --noEmit` (직전 세션에서 교체, 이번 세션 검증)
  - Layer 2/3 pre-commit `typecheck + test` (이번 세션 §8 항목 2)
  - Layer 5 3+1 서브에이전트 (`.claude/agents/`, 이번 세션 §8 항목 4)
- **주의**: `.claude/settings.json` (Layer 1 훅 + Notion 권한) 및 `.claude/agents/` 변경은
  Claude Code 의 settings 로드 시점 특성상 **다음 세션부터 완전 활성**일 가능성. 현재 세션
  컨텍스트엔 이전 상태가 유지될 수 있음.
- **미커밋**: 없음 (세션 말 `.claude/settings.local.json` 의 하네스 자동 추가 라인은
  `git checkout --` 로 원복 — 커밋된 48줄 상태 유지).

### 하네스 점검에서 드러난 후속 과제 (이번 세션 범위 밖)

1. `.claude/settings.local.json` 이 tracked + gitignore 없음 — 매 세션 자동 추가로 diff 발생.
   Claude Code 공식 관례는 gitignore. 별도 결정 필요.
2. `npm install` 시 audit 46 vuln (기존 + 신규 혼재). 별도 audit 세션에서 분류.
3. ESLint pre-commit 연결은 의도적으로 미실시 — 규칙 합의 과정 필요해질 때 Layer 2 에
   `npm run lint` 단계 추가.
