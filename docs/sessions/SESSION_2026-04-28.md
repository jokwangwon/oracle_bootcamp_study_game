# 세션 로그 — 2026-04-28 (Session 9)

## 한줄 요약

**ADR-020 PR-8 Tailwind 마이그레이션 + 메인 페이지 디자인 브리프 → 시안 D 통합 완성** (3 PR 머지: #33 stack 복구 + #34 PR-8 + #35 PR-8b 시안 D 글라스 통합본). 외부 도구/디자이너 핸드오프용 디자인 브리프 v1 + 시안 D 구현 명세 v1 신규 작성. Apple Vision 글라스 톤(사용자 결정) 으로 Hero 3-layer 패널 + Journey strip + 비대칭 카드 페이지 배경 블롭과 함께 적용.

---

## 1. 세션 시작 — main 상태 점검 (Session 8 잔여)

Session 8 종료 시점 main 위 5 PR open 으로 기록됐으나, 실제 점검 결과:

| PR | 상태 | 발견 사항 |
|---|---|---|
| #27 (PR-6 auth rate limit) | MERGED | 정상 |
| #28 (PR-1 Tailwind 인프라) | MERGED | 정상 |
| #29 (PR-2 theme token) | MERGED — but **main 미반영** | base = `feature/adr-020-pr1-tailwind-infra` (stack 부모) |
| #30 (PR-4 next-themes) | MERGED — but main 미반영 | base = `feature/adr-020-pr2-theme-token-bridge` |
| #31 (PR-5 shadcn init) | MERGED — but main 미반영 | base = `feature/adr-020-pr4-next-themes-toggle` |

**원인**: stack PR base 가 부모 feature 브랜치였고, PR-1 main 머지 후 GitHub UI 가 base 자동 갱신을 하지 않은 채 부모 feature 에 머지 → main 에는 변경 누락.

---

## 2. PR #33 — stack 머지 누락 복구

**브랜치**: `recovery/adr-020-pr2-pr4-pr5` (main 기준)

PR head ref 가 살아있어 PR-1 위에 깨끗하게 쌓인 3 커밋을 main 에 cherry-pick.

| 원 PR | 원 커밋 | 복구 커밋 |
|---|---|---|
| #29 (PR-2) | `faa6c43` | `fbe832d` (theme token + light) |
| #30 (PR-4) | `1dea347` | `7ad3957` (next-themes + Header 토글) |
| #31 (PR-5) | `dcf2756` | `2dea9eb` (shadcn init + 6 컴포넌트 + `--brand` rename) |

게이트: typecheck OK / `next build` 8/8 / 컨테이너 200 OK / CSS 토큰 light/dark 양쪽 deploy 검증.

원 PR #29/#30/#31 은 MERGED 상태로 GitHub 기록 보존.

---

## 3. PR #34 — PR-8 Header + 홈 + 로그인/회원가입 Tailwind 마이그레이션

**브랜치**: `feature/adr-020-pr8-header-home-auth` (main 기준)

ADR-020 §6 PR-8 — inline-style → Tailwind utility + shadcn 컴포넌트 재작성. 카드/배너/색감 첫 본격 시각 변경.

### 변경

| 파일 | 변경 |
|---|---|
| `Header.tsx` | inline-style → Tailwind, shadcn `Button` (ghost/outline/brand) + asChild Link, hover transition |
| `app/page.tsx` | 카드 그리드 → shadcn `Card` + lucide 아이콘, hover translate/border/shadow, primary 카드 상단 그라디언트 |
| `app/login/page.tsx` | shadcn `Card` + `Input` + `Label` + `Button`, autoComplete, role=alert |
| `app/register/page.tsx` | 동일 패턴 + 보조 안내 텍스트 |

### 게이트
- 타입체크 OK / `next build` 8/8 static pages
- 사이즈: `/` 175 B → 855 B / `/login` 2.71 → 3.92 kB / `/register` 2.78 → 4.04 kB
- 935 → 955 + 1 skipped tests GREEN
- 컨테이너 200 OK (`/`, `/login`, `/register`)

---

## 4. 사용자 평가 → 메인 페이지 재설계 트랙 진입

PR-8 머지 후 사용자 피드백:

> "메인 페이지 부터 수정이 필요할 듯 합니다. 해당 설계를 바탕으로 다른 곳에서 디자인을 생각해볼 수 있도록 정리해줄 수 있나요?"

→ 외부 도구(v0.dev / Magic UI / 21st.dev / Figma)나 디자이너 핸드오프용 **디자인 브리프** 작성 결정.

### 4.1 신규 문서 — `docs/rationale/main-page-design-brief.md` v1

13개 섹션 구성:
- 프로젝트 한 줄 (한/영) / 페르소나 / 현재 페이지 구조 ASCII + 콘텐츠 인벤토리
- 사용자 평가 (밋밋함) 6 약점 분해
- 기술 제약 (변경 가능/불가) / 디자인 토큰 (PR-2 hex + shadcn HSL) / WCAG 6항 / 변경 허용 범위 3단계
- 영감원 후보 + 분위기 키워드 (한/영) + 피해야 할 것
- **외부 도구 prompt 템플릿 3종** (v0.dev 영어 / ChatGPT 한국어 / Figma 디자이너)

INDEX.md `rationale/` 섹션 등록.

### 4.2 사용자 외부 도구로 시안 D 도출 → 핸드오프 명세 작성

사용자가 외부에서 시안 D ("라이브 코호트 + SQL 에디터 도메인 anchor + 부트캠프 여정" 하이브리드) 를 결정해 가져옴.

→ `docs/rationale/main-page-redesign-concept-d.md` v1 신규 (받은 명세 본문 그대로 + 1줄 구현 메모: `tailwind.config.js` `.js` 유지).

15개 섹션: 시각 레이아웃 ASCII / 영역별 상세 / 데이터 contract / ViewModel shape / mock 시작점 / 토큰 매핑 / 신택스 하이라이팅 / WCAG specific / 파일 매핑 / PR 분할 (PR-1~4) / Acceptance / 변경 정리 / 미해결 / 외부 핸드오프 / 변경 기록.

INDEX.md 등록.

### 4.3 진행 옵션 결정 (사용자 답변 (b))

옵션 (a) 저장만 / **(b) 본 세션에서 PR-1~3 차례 진행** / (c) PR-1만 → 외부 평가.

사용자 추천대로 (b) + `tailwind.config.js` 그대로 유지 (`.ts` 마이그레이션 패스).

---

## 5. PR #35 — PR-8b 시안 D 통합 (3 commit)

**브랜치**: `feature/adr-020-pr8b-pr1-hero-redesign` (main 기준)

처음에는 concept-d §10 분할 (PR-1~3) 대로 시작했으나, 사용자가 글라스 톤 결정 후 옵션 (β) "한 PR 묶기" 채택 → 같은 브랜치에 3 commit 누적.

### 5.1 Commit 1 — `4801913` (PR-1 토큰 + Hero)

**문서 / ADR**:
- `docs/rationale/main-page-design-brief.md` 신규 (§4.1)
- `docs/rationale/main-page-redesign-concept-d.md` 신규 (§4.2)
- ADR-020 §11 변경 기록 2행 추가 (PR-2/PR-5 토큰 rename + PR-8b 토큰 5종)

**토큰** (concept-d §5):
- `globals.css`: `--brand-strong` (light `#0369a1` / dark `#0ea5e9`) / `--code-bg` (`#0f172a` always-dark) / `--code-tab-bg` (`#1e293b`) / `--syntax-blank` (`#fde047`) / `--syntax-blank-fg` (`#422006`)
- `tailwind.config.js`: `brand-strong` / `code{,.tab}` / `syntax{.blank,.blank-fg}` colors, `brand-gradient` backgroundImage (135deg, brand → brand-strong), `grid-cols-20` gridTemplateColumns

**컴포넌트**:
- `lib/home/types.ts` — `HomeViewModel` + 5 sub-types (HeroData / TodayQuestion / LiveTicker / JourneyData / CardsData)
- `lib/home/mock.ts` — `authedMock` + `guestMock` (concept-d §4.3 수치 그대로)
- `lib/home/data.ts` — `getHomeViewModelClient` (PR-10 이전 임시 client-side, JSDoc 에 RSC 환원 시점 명시)
- `components/home/hero-live-panel.tsx` — Hero 좌(개인화 카피) + 우(3-layer)
- `app/page.tsx` — `'use client'` 전환 + HeroLivePanel 통합. 카드는 PR-8 그대로 유지

**3-layer 우측 패널**:
| Layer | 배경 | 내용 |
|---|---|---|
| 1 탭바 | `bg-code-tab` (`#1e293b`) | ● filename + 모드 라벨 |
| 2 코드 | `bg-code` (`#0f172a` always-dark) | 라인 넘버 + syntax (purple-400 keyword / sky-400 fn / yellow-300 blank) |
| 3 티커 | `bg-bg-elevated` | 인증: 활성·1위·정답률 / 게스트: 정적 카피 |

### 5.2 Commit 2 — `6a6c158` (Apple Vision 글라스 톤, 사용자 결정)

사용자 질문: "색감을 글라스 재질로 바꿀 수 있나요?"

→ 옵션 제시 (적용 영역 1/2/3 + 톤 A/B/C). 사용자 결정: **C 스타일 (Apple Vision) + 1 (Hero 패널만 + 배경 블롭)**.

**페이지 배경** (`globals.css`):
- `body::before` fixed pseudo-element + radial-gradient 2개 (좌상단 brand `0.18` + 우상단 brand-strong `0.15` light)
- dark: brand `0.20` + brand-strong `0.18`
- `pointer-events:none`, `z-index:-1`

**Hero 패널** (`hero-live-panel.tsx`):
- 외곽: `bg-bg-elevated/55 backdrop-blur-2xl border-white/15 ring-1 ring-inset ring-white/10` + brand-tinted shadow `0 24px 70px -20px rgba(brand 0.35)` (light) / `rgba(brand 0.28)` (dark, ring-white/[0.07])
- 상단 1px 광택 `via-white/40` (Apple Vision 시그니처)
- Layer 1: `bg-code-tab/75 backdrop-blur-md` + 상단 보더 `border-white/5`
- Layer 2: `bg-code/95` (가독성 우선, 거의 불투명)
- Layer 3: `bg-white/30 backdrop-blur-xl` (light) / `bg-white/[0.04]` (dark) + `border-white/10`
- 활성 도트 미세 success-tinted glow (`box-shadow`)

### 5.3 Commit 3 — `93cd70d` (Journey strip + 비대칭 카드)

**JourneyStrip** (concept-d §3.3):
- `grid-cols-20 gap-0.5` 20-day 진행 막대
- 4 상태 색 매핑: done `bg-brand` / recent `bg-brand/60` / today `bg-amber-500 ring-amber-200` / upcoming `bg-border`
- 색 + 위치 + 라벨 3중 인코딩 (WCAG 1.4.1)
- 글라스 컨테이너 (Hero 톤 통일) + 상단 광택
- `role="img"` + `aria-label` 의미 전달 (스크린리더)
- 게스트 분기: 모든 막대 회색 + "부트캠프 미리보기" 라벨

**FeatureCards** (concept-d §3.4):
- `lg:grid-cols-[1.4fr_1fr_1fr]` 비대칭 / `sm:grid-cols-3` 균등 / mobile 1열
- **Primary** `솔로 플레이`: `bg-brand-gradient` + 모드 chips (`['빈칸','용어','MC','+2']`) + 챕터 진행 mini 박스 (`bg-white/14`, progress bar width transition) + ArrowRight hover translate
- **Ranking** `랭킹`: 글라스 + 헤더 amber Trophy + top 2 + 내 위치 `bg-brand/10` highlight (게스트 fallback "로그인하면 내 순위 표시")
- **Admin** `학습 범위`: 글라스 + Lock 아이콘 + "🔒 관리자 전용" 푸터 (`fg-muted/80`)
- 모든 카드 hover: `-translate-y-0.5 + border-brand/40`
- 카드 통째 `<Link>` + focus-visible `ring-brand`

**`page.tsx`**:
- HeroLivePanel + JourneyStrip + FeatureCards 통합
- mounted 후 vm + authed 동시 분기 (`!authed` → JourneyStrip `isGuest=true`)
- 기존 PR-8 카드 그리드 코드 제거 (FeatureCards 가 대체)

### 5.4 게이트

- 타입체크 OK / `next build` 8/8 static pages
- Pre-commit (Layer 2): typecheck + 955 + 1 skipped tests GREEN
- 컨테이너 재빌드 + tailscale `100.102.41.122:3002` HTTP 200
- 홈 페이지 사이즈: 855 B → 6.32 kB (3 commit 누적)
- CSS 검증: `--brand-strong` light=`#0369a1` / dark=`#0ea5e9`, `--code-bg`, `--syntax-blank` 모두 deploy

---

## 6. 의도된 deviation 정리 (vs concept-d)

| 항목 | concept-d 명세 | 본 세션 구현 | 사유 |
|---|---|---|---|
| `page.tsx` | RSC | `'use client'` | localStorage 인증, PR-10 (httpOnly) 후 RSC 환원 |
| Hero 패널 톤 | "다크 패널" | "글라스 + 배경 블롭" | 사용자 결정 (Apple Vision 톤) |
| Ranking/Admin 카드 | "단색 카드" | 글라스 | Hero 와 톤 통일성 |
| `tailwind.config.ts` | 명세 표기 | `tailwind.config.js` 유지 | 본 프로젝트는 `.js`, 마이그레이션은 별도 작업 |

---

## 7. 누적 통계

- **3 PR 머지** (#33 복구 + #34 PR-8 + #35 PR-8b 시안 D 통합)
- **신규 문서 2종**: design-brief v1 + redesign-concept-d v1
- **ADR-020 §11 변경 기록 2행**
- **신규 토큰 5종 + Tailwind 매핑 3종**
- **신규 web 파일 6개**: `lib/home/{types,mock,data}.ts` + `components/home/{hero-live-panel,journey-strip,feature-cards}.tsx`
- **테스트**: 955 + 1 skipped 그대로 (web 테스트 없음, build/typecheck 게이트로 갈음)
- **외부 노트북 검증 4회** (tailscale `100.102.41.122:3002`): #33 / #34 / #35 PR-1 / #35 글라스+Journey+카드

---

## 8. 다음 세션 우선순위

### 0순위 — 시안 D 평가 후속 (조건부)

외부 노트북 평가 결과에 따라:
- (a) 토큰 패치 (색감/스페이싱) — 가벼운 폴리시 PR
- (b) 미세 조정 (대비, 모바일 reflow) — concept-d §13 미해결 항목
- (c) 만족 시 다음 PR 진행

### 1순위 — ADR-020 §6 남은 PR

**보안 게이트 (CRITICAL)** — PR-8b 시각 변경 0 영역, 독립 병렬 가능:
- **PR-3** (CRITICAL-B1) — `helmet` + CSP + Next.js `headers()`
- **PR-7** (CRITICAL-B5) — pre-commit Layer 3-a3 재귀 + pii-regression 확장

**UX 후속**:
- **PR-9** — `/play/solo` Tailwind 마이그레이션 (PR-8b 의 글라스 톤 재사용)

**대규모**:
- **PR-10** — httpOnly 쿠키 + refresh + CSRF + R4 discussion + sanitize-html (CRITICAL-B2 + C-B4 부분, 3d 추정). 머지 시 `lib/home/data.ts` RSC 로 환원 가능
- **PR-11~12** — vote UNIQUE + discussion 페이지 + VoteButton (PR-10 의존)

### 2순위 — 시안 D mock → 실 API 연결

`lib/home/data.ts` 가 mock 반환. 실 데이터 연결 시점:
- `todayQuestion` — 챕터 시드 + AI 생성 후 오늘 챕터 추출
- `ticker` — Phase B 운영 모니터링 데이터 또는 별도 endpoint
- `journey` — `user_progress` 집계
- `cards.primary.chapterProgress` — 동일
- `cards.ranking` — 기존 ranking endpoint (현재 `/rankings` 미구현)
- `streak` — 별도 ADR + 백엔드 PR (concept-d §13)

각 항목은 별도 PR 또는 묶어서 1 PR.

### 3순위 — ADR-019 24h 관측 결과 확인

24h 후 `@Cron(EVERY_DAY_AT_MIDNIGHT)` 자동 실행: `sr_metrics_daily` 1행 INSERT 확인.
SR 혼합 편성 실사용 피드백 (2회차 이후 due 우선 편성).

### 4순위 — Session 8+ 기술 부채 (이월)

- `Round.startedAt: number` 필드 + `timeTakenMs = answer.submittedAt - round.startedAt` + 프론트 epoch ms 전송 (ADR-019 §8.1)
- 해소 후 quality 공식 `timePenalty` 재도입 평가 (현재 Q5=a 로 제거됨)

### 5순위 — Notion / MVP-C / 실시간

CONTEXT.md 의 기존 항목 그대로 이월.

---

## 9. 환경 / 운영 메모

- docker compose `web` — PR-8 + #33 (PR-2/4/5) + #35 (PR-8b) 모두 반영. main 동기화 후 한 번 더 rebuild 권장
- API 컨테이너 — Session 8 PR-6 (#27 auth rate limit) 머지 후 별도 rebuild 진행 (CONTEXT.md 메모)
- `next-env.d.ts` (Next.js 자동 생성) — 매 build 마다 untracked. .gitignore 처리 시점 미정 (Session 8 부터 이월)
- gh CLI `pr edit` Projects classic deprecation 경고로 제목 갱신 실패 — PR description 자체는 commit 본문이 자동 반영되므로 review 영향 없음
- 글라스 톤: `body::before` fixed pseudo-element 가 viewport 가득. 모바일 작은 화면 + 큰 글라스 패널 시 GPU 부하 가능 — 사용자 환경 평가 필요

---

## 10. 본 세션 요약 메트릭

| 항목 | 값 |
|---|---|
| PR 머지 수 | 3 (#33 / #34 / #35) |
| 새 commit 수 | 7 (cherry-pick 3 + PR-8 1 + PR-8b 3) |
| 새 파일 수 | 12 (web 9 + docs 3) |
| ADR 변경 | ADR-020 §11 변경 기록 2행 |
| 테스트 변화 | 935 → 955 + 1 skipped (이전 세션 누적, 본 세션 web 변경 only 라 0) |
| 외부 노트북 검증 | 4회 |
| 사용자 결정 | 옵션 α/A/B 4건 (PR-8b 진행 / 디자인 브리프 / 시안 D / 글라스 톤 / 1+C / 옵션 (β)) |
