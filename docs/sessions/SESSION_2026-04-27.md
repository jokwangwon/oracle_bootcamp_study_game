# 세션 로그 — 2026-04-27 (Session 8)

## 한줄 요약

**ADR-020 12 PR 로드맵 본격 착수 — 5 PR open (#27 / #28-29-30-31 4-stack) · 936 → 955 tests (+19) · 외부 노트북 검증 완료**. PR-6 (auth rate limit, CRITICAL-B3) 독립 + PR-1 (Tailwind 인프라) → PR-2 (theme token + light) → PR-4 (next-themes 토글) → PR-5 (shadcn init + 6 컴포넌트, `--accent`→`--brand` rename 22 위치) 4-stack. tailscale `100.102.41.122:3002` 재빌드 2회 (PR-4 + PR-5) 로 사용자 노트북 평가 사이클 시작. 사용자 디자인 평가 ("카드/배너/색감 밋밋") → 진짜 시각 변경은 PR-8 부터.

---

## 1. 세션 시작 — main 동기화 (PR #25 / #26 머지 확인)

`docs/adr-020-ux-redesign` 브랜치 → main fast-forward (`cecc1ba`).

| PR | 머지 커밋 | 내용 |
|---|---|---|
| #25 | `77fb133` | 오답 노트 + UX v2 |
| #26 | `cecc1ba` | ADR-020 UX 재설계 초안 + consensus-009 |

신규 main 자료: `user-mistakes.service.{ts,test.ts}` / `review/mistakes/page.tsx` / `ADR-020-ux-redesign.md` / `consensus-009-ux-redesign.md`.

---

## 2. PR #27 — PR-6 auth rate limit (CRITICAL-B3)

**브랜치**: `feature/adr-020-pr6-auth-rate-limit` (main 기준 독립)
**테스트**: 936 → **955 + 1 skipped (+19)**

ADR-020 §4.3 / consensus-009 CRITICAL-B3 (auth brute-force).

### 변경 요약

- `@nestjs/throttler@^6.5.0` 추가
- **`AuthModule`** `ThrottlerModule.forRoot([{name:'login', ttl:900_000, limit:5}, {name:'register', ttl:3_600_000, limit:3}])`
- **`AuthController`** `@UseGuards(ThrottlerGuard)` + 메서드별 `@Throttle({...})`
- **`RedisRateLimiter`** 일반화 (`apps/api/src/modules/shared/rate-limit/`)
  - kind ∈ `appeal | post | comment | vote`
  - window ∈ `second | minute | hour | day` (kind 별 일부 사용)
  - key 구조 통일 `rate:{kind}:{key}:{window}:{bucket}` (좁은 window 우선 reason)
- **`AppealRateLimiter`** thin wrapper 재작성 — ADR-016 호환 회귀 0 (key `min:` → `minute:` 일관 정렬만 변경)

### 테스트

| 파일 | 케이스 |
|---|---|
| `redis-rate-limiter.test.ts` (신규) | 14 (4 kind × 다중 window + 빈 limits 방어 + reason 우선순위) |
| `auth-throttler.test.ts` (신규) | 5 (login/register 메타데이터 + 독립 카운터 + Guard 등록) |
| `appeal-rate-limiter.test.ts` (수정) | 7 회귀 GREEN |

### 알려진 한계

- `vitest+esbuild` 의 `emitDecoratorMetadata` 미지원 → `Test.createTestingModule` 통합 호출 e2e 보류.
- `@Throttle` 데코레이터 메타데이터 검증 + `@nestjs/throttler` 라이브러리 자체 검증 신뢰로 갈음.
- 추후 `unplugin-swc` 도입 시 e2e 추가 검토.

---

## 3. PR #28 — PR-1 Tailwind 인프라

**브랜치**: `feature/adr-020-pr1-tailwind-infra` (main 기준)

ADR-020 §3.1 / §6 PR-1.

### 변경 요약

- 의존성:
  - dev: `tailwindcss@^3.4` / `postcss` / `autoprefixer`
  - runtime: `clsx` / `tailwind-merge` / `class-variance-authority` / `lucide-react`
- `tailwind.config.js` (`darkMode: 'class'`, `content: ['./src/**/*.{ts,tsx}']`)
- `postcss.config.js` (tailwindcss + autoprefixer)
- `globals.css` 상단에 `@tailwind base/components/utilities` 3줄 prepend
- `src/lib/utils.ts` (`cn = twMerge(clsx(...))`)

### 게이트
- typecheck OK / Next.js build OK / 8/8 static pages 정상
- UI 회귀 0 (utility 클래스 미사용 — 인프라만)

---

## 4. PR #29 — PR-2 theme token 브리지 + light 팔레트

**브랜치**: `feature/adr-020-pr2-theme-token-bridge` (PR-1 위 stacked)

ADR-020 §3.1 / §3.3 / §6 PR-2.

### 변경 요약

- `globals.css`:
  - `:root` → light (`#fff` / `#f8fafc` / `#0f172a` / `#64748b` / `#0284c7` / `#e2e8f0`)
  - `.dark` → dark (기존 `:root` 값)
  - success/error 도 light/dark 양쪽 (light: `#16a34a`/`#dc2626` / dark: `#4ade80`/`#f87171`)
  - **신규 `--accent-fg`** (light=`#fff`, dark=`#0f172a`) — accent 위 텍스트 WCAG 2.1 AA 4.5:1+ 보장
- `tailwind.config.js` `theme.extend.colors` 9종 매핑 (`bg`, `bg-elevated`, `fg`, `fg-muted`, `accent`, `accent-fg`, `success`, `error`, `border`)
- inline-style hardcoded `#0f172a` **7곳 → `var(--accent-fg)`** (Header / ReviewBadge / login / register / play/solo / review/mistakes)

### WCAG 보강 근거

ADR-020 §3.3 의 light accent `#0284c7` + 기존 hardcoded `#0f172a` 조합이 대비비 3.0:1 → AA 미달. `--accent-fg` 토큰으로 해소. ADR-020 §3.3 spec 후속 패치 권고.

---

## 5. PR #30 — PR-4 next-themes + Header 토글

**브랜치**: `feature/adr-020-pr4-next-themes-toggle` (PR-2 위 stacked)

ADR-020 §3.3 / §7 / §6 PR-4. PR-2 의 light only 상태 해소.

### 변경 요약

- `next-themes@^0.4` 추가
- **`src/components/theme-provider.tsx`** — `'use client'` 래퍼 (server component 인 layout 에서 children wrap 위함)
- **`src/components/ThemeToggle.tsx`** — Sun/Moon (lucide-react) + `mounted` 가드 + `aria-label` / `aria-pressed` (WCAG 4.1.2)
- **`src/app/layout.tsx`** — `<html suppressHydrationWarning>` + `<ThemeProvider attribute='class' defaultTheme='light' enableSystem>`
- **`src/components/Header.tsx`** — 우측 액션 영역 시작에 `<ThemeToggle />`

### 옵션 근거

- `attribute='class'` — Tailwind `darkMode: 'class'` 와 일치
- `defaultTheme='light'` — Q15=b (programmers 지향)
- `enableSystem` — OS `prefers-color-scheme` 존중 + 토글 override
- `suppressHydrationWarning` — next-themes 의 `<html>` class 동적 토글에 대한 정상 처리

---

## 6. 외부 노트북 검증 #1 (PR-4 적용)

사용자가 외부 노트북에서 평가 요청. tailscale `100.102.41.122:3002` 로 접속 가능하도록 docker compose `web` 컨테이너 rebuild.

```
sudo docker compose build web
sudo docker compose up -d web
```

검증:
- HTTP 200 OK (tailscale 14ms)
- next-themes 인라인 스크립트 head 주입 (FOUC 방어)
- `defaultTheme='light'` 적용
- ThemeToggle 헤더 우측 끝
- light/dark 팔레트 둘 다 빌드 (`/_next/static/css/...`)

사용자 피드백: **"카드/배너/색감 등 디자인이 밋밋"**. 현재 단계는 인프라+토큰만 (utility 미사용) → PR-5 (shadcn) + PR-8 (페이지 마이그레이션) 부터 본격 시각 변경.

---

## 7. 디자인 방향 결정 — ADR-020 그대로 유지 + MCP 도입 보류

옵션 제시:
1. ADR-020 그대로 진행 → PR-5/PR-8 까지 가서 시각 평가
2. MCP 도입을 ADR 결정으로 격상 (3+1 합의)
3. TweakCN 등 가벼운 색감 보강

**사용자 결정**: **방향 1** (그대로 진행, "진행하면서 수정"). 디자인 관련 MCP 후보 (shadcn registry / 21st.dev / Magic UI / TweakCN / v0.dev / Figma MCP) 는 후속 평가용으로만 기록.

---

## 8. PR #31 — PR-5 shadcn init + 6 컴포넌트 + 토큰 충돌 해소

**브랜치**: `feature/adr-020-pr5-shadcn-init` (PR-4 위 stacked, **4-stack**)

ADR-020 §3.2 / §6 PR-5.

### 토큰 충돌 (PR-2 ↔ shadcn 표준)

| 토큰 | 우리 의미 (PR-2) | shadcn 의미 |
|---|---|---|
| `--accent` | brand primary (blue, `#0284c7`) | hover bg (light grey, `210 40% 96.1%`) |
| `--accent-fg` | brand 위 텍스트 | (해당 없음 — `--accent-foreground` 별도) |

**해소 결정**: 우리 `--accent` / `--accent-fg` → `--brand` / `--brand-fg` rename. shadcn 표준 토큰 그대로 수용. ADR-020 §3.3 spec 후속 패치 권고.

### 변경 요약

#### shadcn 인프라
- `apps/web/components.json` (new-york / slate / cssVariables / lucide)
- `apps/web/src/components/ui/` 6개:
  - `button.tsx` / `card.tsx` / `dialog.tsx` / `input.tsx` / `label.tsx` / `dropdown-menu.tsx`
- `tailwindcss-animate` 플러그인 (Dialog/DropdownMenu keyframe)

#### 두 토큰 시스템 공존 (`globals.css`)
- **PR-2 hex 토큰** (페이지 inline-style 호환): `--bg`, `--bg-elevated`, `--fg`, `--fg-muted`, **`--brand`**, **`--brand-fg`**, `--success`, `--error`, `--border`
- **PR-5 shadcn HSL 토큰** (alpha 호환): `--background`, `--foreground`, `--card`, `--popover`, `--primary`, `--secondary`, `--muted`, `--accent` (hover bg!), `--destructive`, `--input`, `--ring`, `--radius`
- light/dark 양쪽 정의 + `--primary` 는 `--brand` 와 동일한 색을 HSL 로 표현

#### `tailwind.config.js` colors
- PR-2 토큰 그대로 + `brand` / `brand-fg` 추가
- shadcn 표준 colors 매핑 (`hsl(var(--primary))`, `hsl(var(--card))`, ...)
- `container` + `borderRadius` + `accordion` keyframes

#### 페이지/컴포넌트 일괄 sed (7 파일)
- `var(--accent)` → `var(--brand)` (12 위치)
- `var(--accent-fg)` → `var(--brand-fg)` (10 위치)
- `Header.tsx` / `ReviewBadge.tsx` / `login/page.tsx` / `register/page.tsx` / `play/solo/page.tsx` / `review/mistakes/page.tsx`

### 게이트
- typecheck OK / Next.js build OK / 8/8 static pages
- 사이즈 변화: 페이지 거의 0, CSS 5.8KB → 17.6KB (Tailwind animate + 토큰 + base)
- 페이지 회귀 0 — `var(--brand)` 가 `var(--accent)` 와 동일 색

---

## 9. 외부 노트북 검증 #2 (PR-5 적용)

docker compose `web` 재빌드 + restart. tailscale `100.102.41.122:3002` 응답 200.

CSS 검증 (light + dark 둘 다 deploy):
```
:root { --bg:#fff; --brand:#0284c7; --primary:199.4 88.7% 39.4%; --accent:210 40% 96.1%; ... }
.dark { --bg:#0f172a; --brand:#38bdf8; --primary:198.4 93.2% 59.6%; --accent:217.2 32.6% 17.5%; ... }
```

PR-5 자체는 컴포넌트 카탈로그만 등록 — 페이지 적용 X. 사용자 노트북 화면은 PR-4 와 시각 동일. **카드/배너/색감 변화는 PR-8 부터 시작**.

---

## 10. 다음 세션 우선순위

### 0순위 — PR #27 / #28 / #29 / #30 / #31 머지

5 PR open. 머지 순서:
1. PR #27 (PR-6 auth) — 독립
2. PR #28 (PR-1) → 머지 시 PR #29 base 자동 main
3. PR #29 (PR-2) → 머지 시 PR #30 base 자동 main
4. PR #30 (PR-4)
5. PR #31 (PR-5)

머지 후 main 동기화 → PR-8 시작.

### 1순위 — PR-8 (Header + 홈 + 로그인 + 회원가입 Tailwind 마이그레이션)

ADR-020 §6 PR-8. **카드/배너/색감 첫 본격 변경**:
- `Header.tsx` inline-style → Tailwind utility + shadcn `Button` (필요 시)
- `app/page.tsx` 홈 카드 그리드 → shadcn `Card` 또는 Tailwind 카드 패턴
- `login/page.tsx` / `register/page.tsx` → shadcn `Input` / `Label` / `Button` + Card 레이아웃

PR-8 머지 후 사용자 노트북 평가 사이클:
- 외부 노트북 시각 평가 → "카드/배너/색감 어디가 약한지" 구체화
- 토큰 패치 (`globals.css`) 또는 컴포넌트 보강
- ADR-020 §3.3 spec 패치 (`--brand` rename 포함)

### 2순위 — 보안 게이트 (독립 병렬)

- **PR-3** (CRITICAL-B1) — `helmet` + CSP
- **PR-7** (CRITICAL-B5) — pre-commit Layer 3-a3 재귀 + pii-regression 확장

### 3순위 — ADR-020 §3.3 spec 패치 (docs)

`--accent` → `--brand` rename 결정과 `--accent-fg` 토큰 추가는 PR-2/PR-5 에서 이행됐지만 ADR-020 §3.3 본문에는 미반영. §11 (변경 기록) 에도 추가 필요.

### 4순위 — 디자인 평가 후속 (조건부)

PR-8 사용자 평가 결과에 따라:
- (a) 색감/타이포 보강 → 토큰 패치 (가벼움)
- (b) 컴포넌트 라이브러리 보강 → 21st.dev / Magic UI MCP 도입 검토 (3+1 합의 가동 가능)
- (c) 디자인 도구 도입 → TweakCN / v0.dev (별도 ADR-022 가칭)

---

## 11. 환경/운영 메모

- docker compose `web` 컨테이너 — PR-1~PR-5 변경 모두 반영 완료. main 머지 후 한 번 더 rebuild 권장.
- next-env.d.ts (Next.js 자동 생성) — 매 build 마다 untracked. 별도 PR 또는 .gitignore 처리 시점 미정.
- lucide-react@1.11.0 — npm latest. 이전 0.x 버전과 다른 트리. PR-4 ThemeToggle / PR-8 컴포넌트에서 호환 검증 완료.
- API 컨테이너는 PR-6 (auth rate limit) 미적용 상태 (PR #27 미머지). 머지 후 `sudo docker compose build api && up -d api` 필요.

---

## 12. 누적 통계

- **5 PR open** (모두 OPEN, mergeable, CI 미설정으로 statusChecks 비어있음)
- **테스트**: 936 → 955 + 1 skipped (+19)
- **신규 dependencies**: `@nestjs/throttler`, `tailwindcss`, `postcss`, `autoprefixer`, `clsx`, `tailwind-merge`, `class-variance-authority`, `lucide-react`, `next-themes`, `tailwindcss-animate`
- **신규 파일 (web)**: `tailwind.config.js`, `postcss.config.js`, `components.json`, `src/lib/utils.ts`, `src/components/theme-provider.tsx`, `src/components/ThemeToggle.tsx`, `src/components/ui/{button,card,dialog,input,label,dropdown-menu}.tsx`
- **신규 파일 (api)**: `modules/shared/rate-limit/redis-rate-limiter.{ts,test.ts}`, `modules/auth/auth-throttler.test.ts`
- **외부 노트북 검증 2회** (tailscale 100.102.41.122:3002)
